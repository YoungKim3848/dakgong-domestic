# 웹에이전트 핸드오프 — 닥공캠프 국내캠프 사이트

새 세션에서 이 프로젝트를 이어갈 때 반드시 먼저 읽을 것. `README.md`(구조)와 함께 본다.

---

## 2026-08-21 세션 (9) — 선생님·멘토 완료 · 시설 갤러리 · **배포 게이트 해제**

### ★★ 규칙이 바뀌었다 — 이제 커밋하면 바로 push 한다

회장: *"바로바로 푸시해"*. 승인 대기 없이 작업 끝나는 대로 push.
⚠️ `clients/dakgong/STATUS.md` 1-1항은 **아직 옛 규칙(승인 후 push)이 그대로 적혀 있다.**
그 문서를 근거로 대기하지 말 것. 정정은 PM 몫이라 실행층이 안 건드렸다.

### ★ 검사는 손댄 페이지만 돌린다

`--pages <이름>` 없이 돌리면 7페이지 91조합이라 **4분** 걸린다. 이번에 회장이
*"졸라 오래 걸리노"* 라고 했다. 한 페이지는 40초다.

### 배포된 것 (미푸시 0)

| 커밋 | 내용 |
|---|---|
| `29b9f5f` | 선생님 섹션 순서(역할→멘토링→선발) · 6칸 5+1 → 상3/하3 |
| `a945154` | 역할 구성 제목 아래 과목 수업 사진 4장 (프리뷰 A안) |
| `3e78311` | 멘토링 컷 05·06·07 → 03·06·01 |
| `e883a36` | **시설 갤러리 실사진 27장** + 벤토 배열 결함 2건 |
| `050eb20` | 히어로 회색 판 1초 |
| `6530442` `944bf23` | 소제목 7개 폰트 + 락 기준선 이동 |
| `14c3558` | 선생님 다크 섹션 원복 · 지어낸 문구 2건 제거 |

### ★★ 「자리표시일 땐 안 보인다」 — 이번에 세 번 걸렸다

빈 상자로 만들어 둔 것은 **실물이 들어가야 결함이 드러난다.** 세 건 다 같은 종류였다.

1. **시설 갤러리** — 08-03 에 벤토 골격만 만들고 「사진 미삽입」으로 뒀는데, 사진을 넣으니
   배열 4종(p0·p2·p3·p4)이 칸을 4.92:1·0.29:1 로 만들어 **사진이 20~30% 만** 남았다.
   필핀 원본은 배열이 **하나뿐**이고 78~93% 가 남는다. 변주는 국내가 추가한 것이었다.
   → 둘로만 간다. 나누는 근거는 취향이 아니라 **사진 장수**다(11장 = 필핀 배열 / 4장 = 가로 4칸 3:2).
2. **폰 행 높이가 한 번도 안 먹고 있었다** — 선언이 `.p0` 에 걸려 있는데 `.p0` 는 바깥 상자(.fac)고
   그리드는 그 안의 `.bento` 다. **그리드 속성은 그리드 요소에 걸렸는지 확인할 것.**
3. **히어로 회색** — placeholder 배경이 투명이라 흰 바닥 + 검은 오버레이 = 회색이었다.
   `_shell.css` 의 남색 그라디언트는 페이지 CSS 의 `background-image` 가 이미 덮어써 사라져 있었다.

### 남은 일

1. **컨설팅 페이지 — 흰 섹션 4연속.** 섹션 경계가 없다(검사 26건). 배경 교차를 넣으면
   페이지 인상이 바뀌어서 회장 확인 전까지 손대지 않았다.
2. **폰트 4.2MB.** 페이지 전체 4.6MB 중 폰트가 4.2MB(이미지는 0.33MB). 한글 폰트를
   `.otf`/`.ttf` 원본 그대로 부른다. 실측 —
   woff2 로만 3.1MB / woff2 + 상용 2,350자 **0.77MB** / 실제 쓰는 글자만 0.26MB.
   사이트 7페이지가 쓰는 한글 539자가 2,350자 안에 **전부** 들어 있다(밖 0자).
   선언이 🔒 `tokens-domestic.css` 라 회장 지시 대기.
   · 겸사겸사 — 구글에서 **Poppins** 를 7페이지 전부에서 받아온다(쓰는 곳은 헤더·버튼 2곳).
     Font Awesome 115KB 도 아이콘 몇 개만 쓴다.
3. **도메인 `www.dakgongcamp-korea.com` — 아직 등록조차 안 된 도메인이다**(whois `No match`).
   사야 매핑할 수 있다. 필핀 `dakgongcamp.com` 은 **가비아**(2026-06-18 등록).
   사고 나면 ①Netlify 에 도메인 추가 ②가비아에 `www` CNAME → `dakgong-domestic.netlify.app`,
   `@` A → Netlify 가 띄워 주는 IP. Netlify CLI·MCP 는 이 세션에서 인증이 안 걸려 있었다.
4. **선생님·멘토 페이지에 못 채운 칸 2개** (자료 부족, 지어낼 수 없음)
   · 실명·약력 0명 — 자료 원문에도 "확정 후 채워 넣을 수 있도록 **빈칸 형태로 구성**" 이라 적혀 있다.
     공개 여부부터 닥공 확인 필요. 실명이 어려우면 익명 프로필(○○대 수학과, N년차)도 가능한지.
   · 영어·국어·과학은 **사진만 있고 설명이 없다.** 카드 6칸이 수학 2 + 멘토 4라 들어갈 칸이 없다.
     그 세 과목 선생님의 담당·핵심 문구가 자료에 없다.
   · 그래서 이 페이지가 본문 3섹션으로 짧다(생활 12 · 커리큘럼 7). 억지로 늘리면 지어내야 한다.
5. **활동사진 55장·시설 원본(content/thumb) 여전히 미추적** — 쓰는 것만 파생본으로 커밋해 왔다.
6. 05·07 멘토링 파생본은 쓰는 곳 0곳인데 안 지웠다. 정리 여부는 회장 판단.

### 도구(web agent/verify)에 추가된 예외 통로 2개

검사를 약하게 하는 대신, **사유를 적어야만 빼 주는** 통로를 뒀다(대비 예외와 같은 방식).
· `lastrow_exempt` — 「마지막 줄 한 칸」이 글머리표 목록·범례까지 잡았다.
· `flatcard_exempt` — 「카드가 바닥과 같은 색」이 **자기가 곧 바닥인 큰 껍데기**까지 잡았다.

---

## 2026-08-21 세션 (8) — 커리큘럼 페이지 완성 (**회장 오케이**) · 다음은 선생님·멘토

### 진행 방식은 세션 (7) 과 같다

회장이 `http://localhost:8788` 을 보면서 한 건씩 지시한다. **지시받은 것만 하고 멈춘다.**
서버는 반드시 `verify/serve_nocache.py` 로 띄운다(`launch.json` 이 그걸 쓴다).

### 배포된 것 (미푸시 0)

| 커밋 | 내용 |
|---|---|
| `0048a31` | 레벨테스트 사진을 반편성 밑으로 · 6대핵심 헤드+벌집 배경 통일 |
| `9d3ad59` `c087c74` | 카드가 안 보이던 것(배경 동색) · 모바일 스텝 |
| `e80af64` `9bc9c7d` | 섹션 순서 재배치 · 단계 흐름을 상자에서 선으로 |
| `5c31219` | 제목 두 개가 옆 섹션과 같은 말을 하고 있었다 |
| `8bb3a90` | 중복 제거가 과해 제목이 묻는 질문의 답을 지웠던 것 복구 |
| `0f2b404` | 하루 9시간 표기가 원문을 배신하고 있었다 |

### 최종 섹션 순서 (회장 지정) — 배경은 흰/회색 교차

```
6대 핵심 수업 구성 → 레벨 테스트 → 배정 기준 → 실제 반 구성
→ 하루 9시간 이상 → 관리 포인트 → 학습 보완 → CTA
```

⚠️ **순서를 바꾸면 배경 교차가 깨진다.** 그냥 옮기면 흰색 3연속·회색 3연속이 되어
섹션 경계가 사라진다. 이번엔 두 개(레벨 테스트·하루 9시간)를 뒤집어 맞췄다.

### ★★ 「안 보인다」의 원인이 취향이 아니라 **값이 같아서**인 경우

회장: *"이 두 섹션 카드 디자인 시야에 졸라 안들어온다"*. 재보니

| 섹션 | 카드 배경 | 섹션 배경 |
|---|---|---|
| 실제 반 구성 | `#f5f5f5` | **`#f5f5f5`** |
| 학습 보완 | `#fff` | **`#fff`** |

**카드와 바닥이 같은 색**이고 1px 헤어라인만 남아 있었다. 홈에서 "아이콘 원이랑 카드가
같은 색이라 안 보인다"고 지적받은 것과 같은 종류다.
→ 그때 회장이 고른 2안(그림자로 띄우기)을 그대로 썼다. 값은 `.dk-ds` 에서 복제.
**「안 보인다」는 지적이 오면 먼저 카드 배경과 섹션 배경을 나란히 찍어 본다.**

### ★ 사진 블록은 섹션 '안'에 둔다

`.dk-ph-block` 은 섹션 **밖** 형제로 놓는 부품이고 자기 padding 이 56~96px 이다.
위 섹션의 padding-bottom 과 겹쳐 텍스트-사진 간격이 **최대 192px** 까지 벌어졌다.
더 큰 문제는 **소속**이었다 — 섹션 사이에 떠 있으면 위 것인지 아래 것인지 알 수 없어
「배정 기준」에서 실제로 **[사진][텍스트][카드]** 순서로 보였다.
회장: *"첨부 2와 같은 듣도보도 못한 배치는 뭐냐"*.
→ `.page-curriculum-inph` 로 섹션 `.dk-wrap` 안에 넣고 간격은 `gap-lg` 로만.

### ★ 세로 flex 에서 `flex: 1 1 140px` 의 140px 은 **폭이 아니라 높이**다

모바일 스텝 카드를 한 줄짜리 행으로 바꿨는데 높이가 안 내려갔다. 내용만 줄고
빈 공간이 아래로 옮겨간 셈이었다. `flex: 0 0 auto` 로 풀어야 180px → 56px.

### ★ 접히는 폭을 실측하지 않으면 태블릿에서 같은 문제가 그대로 난다

단계 흐름 6칸이 한 줄에 서는 것은 **1100px 부터**다. 769~1099 에서는 5칸+1칸으로 접혀
둘째 줄 오른쪽이 통째로 빈다 — 회장이 지적한 그 모양이 태블릿 폭에서 그대로 났다.
→ 세로 전환 기준을 768 → **1099** 로 올렸다. **한 줄로 못 서는 폭이면 처음부터 세로로.**

### ★ 중복 제거는 같은 말을 지우는 것이지 **문맥에 필요한 말을 지우는 게 아니다**

레벨 테스트/배정 기준 두 섹션의 중복을 없앤다고 "필요하면 자체 테스트를 진행하고"를
위 섹션으로 올렸더니, 배정 기준 제목이 「무엇을 보고 레벨을 나누나」인데 리드에
상담과 사후 조정만 남았다. **질문은 남기고 답을 지운 꼴**이었다.
회장: *"배정기준에 레벨테스트를 빼먹으면 말이 안 되는거 아냐"*.

### ★★ 「9시간」 — 원문이 경고한 것을 웹이 그대로 어기고 있었다

닥공 답변서 원문(`clients/dakgong/자료/2026-07-20_..._답변서.md` 「1. 하루 9시간 학습 구성」):

> "'하루 9시간'을 전부 강의시간으로 표현하기보다, 수업·과제·문제풀이·오답관리를
>  포함한 총 학습량으로 표현하는 것이 정확합니다."

- **「이상」이 커리큘럼에서만 빠져 있었다.** 나머지 6페이지는 전부 「하루 9시간 이상」.
- **국어·영어는 주말인데** 배지가 「약 1시간」뿐이라 평일 합산으로 읽혔다 → 「주말 약 1시간」.
- **「8타임」 제거**(커리큘럼 + 홈 FAQ Q.05). 「타임」은 닥공 내부 단위(=교시)로,
  필리핀 QA 원문에 "영어 10타임, 수학 1타임, 자습 3타임"(= 하루 14타임)으로 쓰인다.
  ⚠️ **1타임이 몇 분인지는 어느 문서에도 없고 두 캠프가 다르다** —
  필리핀 14타임/일·6주 500시간이면 약 1시간, 국내 6시간/8타임이면 45분.
  회장 판단: *"고객이 보기에 말이 안되는 것만 없음 되지"*. 6시간이 상위 표현이라 손실 없음.

### ★ 닥공 원본 자료는 어디 있나 (회장이 "지웠나"라고 물었다 — 안 지웠다)

```
clients\dakgong\자료\                 정리본 md 5개 + README
clients\dakgong\자료
aw6-07\    원본 21개 (워드 3 · txt 6 · html 3 · md 9)
clients\dakgong\imweb\필리핀_캠프_QA_관리자전달용_정리본.docx
```
- 8타임·9시간 원문 = `자료/2026-07-20_..._답변서.md` 22행 · `..._FAQ_40문항.md` 28행
- ⚠️ **국내캠프 PDF 는 이 저장소에 처음부터 없다.** 회장이 갖고 있으면 경로 확인 필요.

---

## 다음 세션 — 선생님·멘토 페이지

### 바로 할 일

0. **⚠️ `verify` 새 검사가 teachers 에서 1건을 잡아 놓았다** — `.dk-kp-card` 가
   다크 섹션(`#2a2a2a`) 위에 **같은 `#2a2a2a`** 카드다(그림자 없음). 40조합에서 실패로 뜬다.
   이번 세션 「카드 = 바닥 동색」 지적과 같은 종류인지 눈으로 판정하고 처리할 것.
   (도구 커밋 `83d4355` 에서 검사 3종 추가: 카드 동색 · 섹션 배경 3연속 · 마지막 줄 1칸)


1. **`teachers.html` 에 「과목별 항목」 섹션이 아직 없다.** 칠판 사진이 예약돼 있다 —
   회장: *"칠판에 글들 보면 사진 컨셉 파악 안되냐!"* 칠판 내용이 과목을 말해 준다.

   | 과목 | 원본 (미추적) |
   |---|---|
   | 수학 | `assets/images/activity/dakgong-domestic-lecture-01-content.webp` |
   | 영어 | `lecture-02` · `lecture-03` · `lecture-04` |
   | 국어 | `lecture-05` · `lecture-06` |
   | 과학 | `lecture-07` · `lecture-08` |

   `assets/images/teachers/` 에는 지금 멘토링 3장(05·06·07)뿐이다.
   쓸 것만 700/1200 webp 파생본으로 만들어 커밋한다(원본 55장은 계속 미추적).

2. **선생님 페이지는 이전 세션에 롤 섹션을 만들었다가 전부 원복했다.**
   회장: *"우리는 원을 많이 안쓰는데…원복해라 걍"*. **원형 모티프를 다시 꺼내지 말 것.**

### 미결 (회장 확인 대기)

- **「실제 반 구성」 라벨=제목 중복** — `CLASS STRUCTURE | 실제 반 구성` / 제목도 「실제 반 구성」.
  다른 섹션은 전부 라벨=주제어 + 제목=문장인데 여기만 같은 말을 두 번 한다.
  리드가 "최대 구성 예시"이므로 「최대 이렇게까지 열립니다」 같은 안을 제시해 뒀다.
- **모바일 스텝에서 상자를 없앤 것은 형태 변경**이다. 되돌리면 오른쪽 공백이 다시 생긴다.
- **컨설팅 히어로에 발표자가 보인다** — 7/30 「컨설턴트 사진 게재 안 함」 판단과 충돌 여부.
- **문의 페이지 히어로** — 다른 컴포넌트(`.dk-contact-hero`)라 안 건드렸다.
- **홈 인터뷰 위 칸** — 필핀 ep.02 를 회장이 편집해 재업로드 예정.
- **활동사진 55장·시설사진 27장 여전히 미추적** — 쓰는 것만 파생본으로 커밋해 왔다.

### 해소된 것

- ~~커리큘럼 첫 사진 자리(`.dk-ph-split`) 위아래 247px 공백~~ → 그 컴포넌트가 커리큘럼에서 사라졌다(사용 0건).

---

## 2026-08-20 세션 (7) — 실물 영상·사진 대량 투입 · 회장이 화면 보며 하나씩 지시

### 진행 방식이 바뀌었다 — 회장이 로컬에 앉아 실시간으로 본다

`http://localhost:8788` 로 띄워 놓고 회장이 직접 보면서 한 건씩 지시했다.
실행층이 계획을 세워 몰아서 하는 게 아니다. **지시받은 것만 하고 멈춘다.**

### ★★ 캐시 함정 — 여기서 회장 시간을 두 번 날렸다

`python -m http.server` 는 캐시를 허용한다. **HTML·CSS 는 새것인데 JS 만 옛것**인
상태가 되면 증상이 코드 버그처럼 보인다 — 후기 롤링이 통째로 멈춘 게 그거였다
(CSS 애니메이션은 이미 지워졌는데 새 JS 가 안 돌아서).
→ **`verify/serve_nocache.py` 를 도구 레벨에 만들었다.** `launch.json` 이 그걸 쓴다.
   응답에 `Cache-Control: no-store` 가 붙어 F5 만으로 반영된다.
   ⚠️ 이미 캐시에 든 파일은 한 번은 `Ctrl+Shift+R` 로 털어야 한다.

### 넣은 것

| | |
|---|---|
| 홈 히어로 | **실물 배경 영상** (원본 4K HEVC 18MB → H.264 1920 2.3MB / 1280 1.06MB, 오디오 제거) |
| 홈 인터뷰 | 3칸 비대칭 → **2개 상하** + 유튜브 2편(파사드) |
| 홈 후기 | 확대창 제거 · **손으로 미는 관성 슬라이더** |
| 커리큘럼 | 사진 6장 · 벌집 순차 등장 · 상세 카드 **리본 입체** |
| 하위 4페이지 | 히어로 배경 이미지 |

### ★ 「레퍼런스 값을 가져온다」가 「통과한다」는 아니다

히어로 블랙박스로 필핀 실측값(.55/.25/.05)을 그대로 가져왔더니 **리드가 4.40:1 로 미달**.
그 값은 필핀 영상에서 검증된 것이지 우리 영상 기준이 아니다 — 우리 히어로가 더 밝다.
하위 4페이지도 처음 값으로 PC 1건·모바일 3건이 미달이었다. **매번 재고 최소값을 찾는다.**

⚠️ **대비를 글자 '블록'으로 재면 안 된다.** h1 블록은 1,120px 인데 글자는 왼쪽 일부만
차지해서, 글자 없는 오른쪽의 밝은 영상까지 섞여 1.94:1 로 잘못 나온다.
`Range.getClientRects()` 의 **줄 상자**로 재면 3.74:1 이다.

### ★ 레퍼런스는 "요점"을 말로 정리한 뒤 구현한다 — 또 걸렸다

상세 카드 레퍼런스(일정표 리본)를 **두 번 틀렸다.**
1차: 컬러 캡이 **좌우로 번갈아** 나오는 걸 놓치고 "우측 탭"으로만 읽었다.
2차: 구조만 옮기고 **입체감을 통째로 빼먹었다.** 회장: *"디자인을 벤치마킹 하라는게 뭔말인지 몰라?"*
→ 입체는 세 겹이다 — ①캡이 카드 뒤로 접히는 짙은 그림자 ②그 접힘이 카드에 드리우는 그늘
   ③바닥에서 뜨는 2겹 그림자. **하나라도 빠지면 "옆에 놓인 색 막대"가 된다.**

### 실물이 들어와야 드러나는 것들 (자리표시일 땐 안 보인다)

- **슬롯 비율** — 사진 55장이 전부 1.50:1 인데 칸 비율이 제각각이라 51~71% 만 남았다.
  `tri` 는 비율을 거는 쪽이 반대였다(큰 칸 고정 → 작은 칸이 2.17:1 로 벌어짐 + 201px 공백).
- **320px 가로 스크롤** — `min-height:130px` 가 4/3 과 충돌해 폭을 173 으로 밀었다.
  이 폭에선 min-height 와 「75% 이상」을 동시에 못 지킨다.
- **1280px 가로 스크롤** — `.dk-wrap` 이 정확히 그 폭에서 패딩 0 이 되어 캡이 나갈 자리가 없다.

### 남은 일

1. **컨설팅 히어로에 발표자가 보인다** — 7/30 「컨설턴트 사진 게재 안 함」 판단과 충돌하는지 회장 확인 대기.
2. **문의 페이지 히어로** — 다른 컴포넌트(`.dk-contact-hero`, 111~163px)라 안 건드렸다.
3. **홈 인터뷰 위 칸** — 필핀 ep.02 를 회장이 편집해 재업로드 예정.
4. **커리큘럼 첫 사진 자리**(`.dk-ph-split`) — 글이 두 줄뿐이라 PC 에서 위아래 247px 씩 빈다. 미해결.
5. **활동사진 55장·시설사진 27장 여전히 미추적** — 쓰는 것만 파생본으로 커밋했다.

---

## 2026-08-17 세션 (6) — 홈 「한눈에 보기」 재설계 · 라벨 전수 통일 · **배포 3회**

### 배포된 것 (미푸시 0)

| 커밋 | 내용 |
|---|---|
| `2abca84` | 「캠프 한눈에 보기」 재설계 + 5대 핵심역량 카드 섹션 삭제 |
| `4bed84d` | 카드를 사이트 기존 패턴으로 · PHILOSOPHY 수업사진 + 구분선 제거 |
| `99647f5` `c68671d` | 5대 핵심역량 **원복** + 상세 상단 제거 · 라벨 전수 통일 · 락 기준선 |

### ★ 「캠프 한눈에 보기」(.dk-glance) — 새 컴포넌트

PC 가로 5칸(원형 배지가 카드 위로 걸침) / **≤1023px 세로 스택**(아이콘 좌측 + 세로 바).
마크업 하나, PC 에서만 `.dk-glance-txt` 를 `display:contents` 로 풀고 `order` 로 세운다.
그래야 같은 dash 하나가 PC 는 라벨 아래 가로선, 모바일은 아이콘 옆 세로선이 된다.

- **위계를 뒤집었다** — 라벨(대상)은 작게, 값(중1~고3학년)은 크게.
  레퍼런스는 제목이 '주장'이라 컸지만 닥공은 라벨이 '분류'다.
- **아이콘 블랙 · 값 코발트.** 아이콘이 시선을 먼저 먹으면 정보인 값이 밀린다.
  (「면에 코발트 5곳」은 배경 규칙이라 글자·아이콘 색에는 안 걸린다)
- **카드 껍데기는 새로 만들지 않았다** — 전 페이지 실측 결과 연회색(#f5f5f5) 섹션 위에
  이 사이트가 쓰는 건 흰 카드뿐이고 다크 카드는 0곳이었다. `.dk-table-wrap` 조합
  (흰 배경 + radius-md + hairline-soft + `0 2px 10px rgba(20,40,90,.06)`)을 복제했다.
  배경과 흰색이 **1.09:1** 이라 명도로는 안 갈리는데, 사이트는 테두리와 그림자로 풀고 있었다.

### ★★ 배경을 다크로 바꿨다가 되돌린 이력 — 다시 하지 말 것

레퍼런스(딥그린 배경 + 흰 카드)를 그대로 옮기려고 섹션을 `--dk-surface-dark` 로 바꿨다가
회장 지적으로 원복했다. 이유 둘 —
1. 이 사이트는 **흰색과 연회색이 번갈아** 나오고 연회색이 4번(3·6·9·11) 규칙적으로 등장한다.
   연속 두 섹션이 같은 배경인 데가 **한 곳도 없었다.**
2. 바로 아래 「숫자로 보는」의 **통계 블록**이 다크라 1,902px 가 한 덩어리로 붙어 보였다.
   ⚠️ 그 섹션 **자체는 흰색**이다 — `dk-stat-dark-sec` 가 안쪽 블록만 칠한다.
   계산값만 보고 "섹션이 다크"로 오독했었다.

### ⚠️ 5대 핵심역량 — 내가 잘못 지웠다가 원복했다

회장 지시는 **"상세 소개 상단을 지워라"** 였는데 **요약 카드 섹션을 지웠다.** 원복 후 올바르게
상세 상단(`5 POINTS | 5대 핵심 역량` 라벨 + `5가지 핵심 역량` 제목 + 안내문)을 제거했다.
지금 구조 = **요약 카드 5장 → 바로 POINT 01 상세.** 제목이 두 번 나오던 게 해소됐다.

### ★ 섹션 라벨 규칙 — 「영문 | 한글」 (전수 32/32)

전수 조사하니 하위 5페이지 중 4개는 100% 지키는데 **index 4곳 · consulting 4곳**만 깨져
있었다(작업 시점이 갈린 흔적). 8곳에 한글을 붙였다.

- `AT A GLANCE | 기본 구성` · `TRACK RECORD | 운영 실적` · `INTERVIEW | 영상 인터뷰` ·
  `TESTIMONIALS | 받은 메시지`
- `01 WHY | 컨설팅이 필요한 이유` · `02 SYSTEM | 담당 구성` · `03 PROCESS | 진행 순서` ·
  `04 PREPARE | 사전 준비`

★ 한글은 **제목과 겹치는 낱말이 0개**가 되게 골랐다 — 어휘 중복(아래)을 라벨에서 또 만들지
않기 위해서다. `AT A GLANCE | 캠프 개요` 는 제목의 '캠프'와 겹쳐 「기본 구성」으로 바꿨다.
※ Q&A 의 `Ⅰ~Ⅳ` 는 섹션 라벨이 아니라 한 섹션 안의 FAQ 카테고리라 **예외로 둔다.**

**색은 원래 일관됐다** — 36개 중 34개 코발트, 다크 배경 2개는 밝은 코발트. 단 그 2개가
서로 달라(흰색 60% vs `--dk-accent-onDark`) 후자로 통일했다. 🔒 `_shell.css` 한 줄,
회장 승인 후 `lock_baseline` 을 `99647f54` 로 갱신.

### 오늘 잡은 버그 넷 (전부 자리표시/기본값이 실물에서 드러난 유형)

1. **번호 블록 L 자 노치** — 모바일에서 칸의 위 절반(48/96px)만 덮었다. 2열로 바꾸며 값을
   2행에 내렸는데 번호 블록에 `grid-row` 를 안 준 탓. ⚠️ **`1 / -1` 은 안 먹는다** —
   `grid-template-rows` 가 없어 `-1` 이 가리킬 끝선이 없다. `span 2` 로 센다.
2. **768px 에서 카드가 125px 로 찌그러짐** — 모바일 분기가 720px 에서 끝나 태블릿이 5열
   PC 배치를 받았다. 세로 스택을 **1023px** 까지 끌고 간다.
3. **`<figure>` 기본 여백 `margin: 1em 40px`** — 390px 에서 사진이 350 → 268px 로 파먹혔다.
4. **줄 수 측정 오류** — PC 는 값에 `flex:1` 이라 높이가 늘어난다. 높이÷행간으로 세면
   과대계상. `Range.getClientRects().length` 로 센다.

### ★ 카드 높이는 가장 큰 것에 맞춘다 (회장 반복 지적)

PC 는 값의 `flex:1`, 모바일은 격자의 `grid-auto-rows:1fr`. **한쪽만 걸면 안 된다** —
PC 에만 걸고 모바일에 안 걸어 지적받았다.

### 회장 판단 대기 — 카피 (글쓰기팀 넘길 자료)

**① 어휘 중복.** 타이틀에 쓴 낱말을 서브에서 또 쓰는 게 공통 원인이다. 실측 —
- PHILOSOPHY 한 덩어리(5문장): **학습 6 · 캠프 6 · 방학 3 · 입시 3**
- 홈 전체: 학습 43 · 관리 37 · 캠프 36 · 입시 22 · 수학 21
- 다른 페이지도 같은 패턴(curriculum 「관리」3·「구성」3, consulting 「입시」3)

**원인은 웹이 아니라 카피 원본이다.** `clients/dakgong/자료/2026-07-21_닥공캠프_한국_최종본_메인카피수정.md`
(8,145자)가 **캠프 71 · 학습 34 · 입시 27 · 방학 14** 로 그 밀도다. 웹에서 단어만 바꾸면
정본과 사이트가 갈라진다 → **글쓰기팀이 원본을 고쳐야 한다.**

**② 필리핀캠프 시절 후기 6장 혼재** (세션 5 기록 참조) — 이미 라이브에 떠 있다.

### 남은 일

1. **하위 페이지 사진** — `.dk-ph` 자리 14개(curriculum 6 · self-study 4 · teachers 1 ·
   life-care 3)가 비어 있다. 활동사진 54장 대기. **레퍼런스를 받아야 한다**(8/16 건).
2. **시설 갤러리 확대창 z-index** — 모바일에서 ✕ 가 헤더(400)에 가려 안 닫힌다.
   후기 쪽은 420으로 고쳤고 갤러리는 그대로다. 🔒 락 파일이라 승인 필요.
3. **활동사진 정리** — 실제 쓰는 1장(`class-01`)만 커밋했다. 나머지 **54장(13MB)은
   추적 안 됨.** 시설사진 27장(12MB)도 마찬가지. 쓸 자리가 정해지면 커밋.

---

## 2026-08-17 세션 (5) — 홈에 실물 사진 전부 투입 + **배포 완료**

### ⚠️ 배포했다 — 밀려 있던 커밋 20개가 한 번에 나갔다

회장 지시로 push. `3e52724 → 9f69951`. **8/3 세션부터 쌓여 있던 것이 전부 같이 나갔다**
(CSS 락/비락 분리 · 대비 정리 · 시설 갤러리 · 히어로 파손 수정 · 인포그래픽 원복).
라이브 확인: 후기 44칸·POINT 5칸 전부 로드, 4xx/5xx **0건**.

### 이미지는 저장소에 담아 서빙한다 (회장 지시)

외부 CDN 안 쓴다. `assets/images/` 에 커밋해서 같은 도메인에서 받는다.

| | 원본 | 웹 | 비고 |
|---|---|---|---|
| 후기 22장 | 5.6MB JPG | **1.5MB** webp 44개 | card 451KB(20KB평균) + full 1,034KB |
| POINT 5장 | **187MB** JPG(6244x4168) | **532KB** webp 10개 | srcset 700w/1200w · lazy |

⚠️ **`assets/images/activity/`(13MB)·`facility/`(12MB)는 아직 추적 안 됨.**
어느 페이지도 참조하지 않는다(8/16 삽입분이 되돌아갔다). 공개 저장소에 25MB를 넣는 건
되돌리기 어려우니 **쓸 자리가 정해진 뒤에 커밋**하는 걸 권한다.

---

## (세션 5 상세) 홈 후기 자리에 실물 카톡 캡처 22장 (커밋 `c233374`)

### 한 일

닥공이 준 22장(바탕화면 「닥공/국내캠프 리뷰」)을 홈 TESTIMONIALS 롤링밴드에 전량 반영.
placeholder 카드 6장이 돌던 자리다. **커밋만 했고 push 안 했다** (`ahead 18`).

| | |
|---|---|
| 이미지 | `assets/images/review/` — card 22장 451KB + full 22장 1,034KB (webp) |
| 마크업 | `index.html` 후기 트랙 → `.dk-tr-shot` 버튼 22개 ×2(루프 복제) |
| CSS | `page-index.css` — `.dk-tr-card` 폐기, `.dk-tr-shot`·`.dk-galbox` 신설 |
| JS | `assets/js/review.js` 신설 (확대창) |

### ★ 크롭 0 — 글 캡처는 "덜 보이는" 게 아니라 "못 읽는" 것이 된다

원본 비율이 **0.45~0.96 으로 제각각**이라 어떤 고정 비율을 잡아도 누군가는 잘린다.
→ **높이만 고정(420/340/300px)하고 폭은 auto.** 전 폭 실측 **100% 유지**.
사진(1.50 고정)과 달리 여기선 `aspect-ratio` + `cover` 조합 자체가 답이 아니다.

### ★ 확대창이 없으면 후기를 실은 의미가 없다

카드 폭이 200px 안팎이라 카톡 글씨가 안 읽힌다. 카드는 "이만큼 있다"만 말하고
읽히는 건 확대창이다. **시설 갤러리(`gallery.js`/`.dk-galbox`)를 값 그대로 복제**했다(0순위 8-①).

⚠️ **z-index 만 60 → 420 으로 바꿨다.** 헤더가 400, 모바일 메뉴가 401 이라
원본 값이면 **모바일에서 ✕ 가 헤더에 통째로 가려 안 닫힌다**(스크린샷으로 확인 후 고침).
**같은 버그가 `page-life-care.css` 시설 갤러리에 그대로 있다** — 거긴 사진이 없어
아무도 눌러 보지 않았을 뿐이다. `_components.css` 로 승격할 때 같이 고칠 것(🔒 락이라 지금은 못 함).

### ★ 개인정보 4곳 — 22장 중 18장은 이미 가려져 있었고 4장이 빠져 있었다

| 파일 | 노출 |
|---|---|
| `…002050595_01` | 실명 「승현」 (노란 말풍선) |
| `…002050595_07` | 대화방 제목 실명 「민준」 |
| `…002050595_20` | 실명 「성옥언니」 |
| `…002050595_25` | 서울대 합격증 **수험번호** |

**원본 폴더는 안 건드렸다.** 웹용 사본에만 블러를 얹었다(닥공이 쓴 방식과 같게).
가린 뒤 확대해 판독 불가 확인. 「안영희」(대표·푸터 공개), 부산외고 교장 성명(공적 발급자)은 그대로 뒀다.

### ⚠️ 회장 판단 대기 — 필리핀캠프 시절 후기가 섞여 있다

지시대로 **22장 전부** 넣었으나, 내용을 보면 **국내캠프(2020~) 것이 아닌 게 최소 6장**이다.
2016~2019년 대화이고 「필리핀 갔구나」·「영어캠프」·「핸드폰 없이 영어공부 전투적으로」 같은
필핀 어학캠프 맥락이다(`_07`·`_08`·`_13`·`_15`·`_22`, `_09` 도 의심).

**국내캠프 페이지에서 「실제 수료생 후기」로 제시하면 귀속이 흔들린다**(0순위 4 — 수치·실적의 귀속).
빼는 게 맞는지, 아니면 「닥공교육그룹 20년」 틀로 묶어 그대로 두는지는 회장 판단.

명확히 국내/닥공 스파르타 맥락인 것 — 2026-07 4장 · `002050595`(스파르타 공부 시킨 울캠프덕) ·
`_27~29`(닥공갔다오고는… 경남과고) · `055402_08` · `055402_10` · `055402_11`.

### POINT 01~05 사진 (커밋 `9f69951`)

`포인트1~5.jpg` 가 칸과 1:1로 맞았다(수업·멘토링·자습실·생활·컨설팅).
**칸 4:3 vs 사진 1.498:1 → 89% 남는다.** 칸 비율이 전 폭에서 안 바뀌어 PC/모바일이 따로 놀지 않는다.

★ **`.dk .card` 의 padding 이 사진을 25px씩 밀어내고 있었다.**
`tokens-domestic.css` 의 `.dk .card { padding: var(--dk-space-lg) }` 를
`.dk-pt-vis .card` 가 padding 을 선언하지 않아 그대로 물려받은 것.
**자리표시(가운데 정렬된 글자)일 땐 안 보이고 실물 사진이 들어와서야 드러난다** —
칸 590x443 인데 사진은 540x393 이었다. `.is-filled` 에 `padding:0` 으로 잡았다.
`.card` 는 이름이 너무 흔해서 또 물릴 자리다.

색보정은 원본에서 끝나 있어 **인물 프리셋을 다시 얹지 않았다**(두 번 먹이면 피부가 더 노래진다).
alt 은 보이는 것만 적었다 — 04 는 아침 체조 컷이라 「점호」라고 쓰지 않았다.

### 그밖에 눈에 걸린 것 (안 고쳤다)

- 섹션 제목이 **「실제 수료생 후기」인데 내용은 학부모 메시지가 더 많다.** 확정 카피라 임의로 안 고쳤다.
- 홈에 **「한국 스파르타 캠프 5대 핵심 역량」(카드 5개)** 과 **「5가지 핵심 역량」(POINT 01~05)** 이
  제목·내용이 겹친 채 둘 다 있다.
- 핸드오프의 「index 사진 자리 0」은 틀렸었다 — 그 표는 `.dk-ph` 만 셌고 홈은 `.dk-pt-vis` 를 쓴다.
  (이번에 채웠다. 위 POINT 절 참조)
- **다른 페이지의 `.dk-ph` 자리는 여전히 비어 있다** — curriculum 6 · self-study 4 · teachers 1 · life-care 3.
  거긴 `.dk-ph-split`(51%)·`.dk-ph-tri` 크롭 문제가 미해결이라 **레퍼런스를 받아야 한다**(8/16 건).

---

## 2026-08-16 세션 (4) — 활동사진 55장 확보 · 사이트 반영은 **되돌림**

### ⚠️ 결론부터 — 사이트 파일은 세션 시작 시점 그대로다

레이아웃이 계속 어긋나 **회장 지시로 전부 되돌렸다**(`git checkout -- .`).
커밋은 하나도 안 했다. 사진·필터·검사 항목은 남았고, **사이트에 어떻게 넣을지만 미정**이다.

**되돌리면서 같이 날아간 것 — 8/4 시설사진 27장 삽입분.** 오늘 작업이 아니었는데
`life-care.html` 한 파일에 섞여 있어서 함께 되돌아갔다. 백업은 있다:

```
_backup_2026-08-16/all-changes.patch             # 오늘 변경 전체 (656줄) — git apply 로 복원
_backup_2026-08-16/life-care-with-facility.html  # 시설사진 27장 들어간 버전
_backup_2026-08-16/curriculum-gallery.html       # 갤러리 문법으로 짰던 커리큘럼
_backup_2026-08-16/_components-with-gal.css      # 갤러리 CSS 승격 + 비율 고침본
```
시설사진만 되살리려면 `life-care-with-facility.html` 을 `life-care.html` 로 덮으면 된다
(그 파일에는 오늘의 활동사진 2장도 같이 들어 있으니 확인하고 쓸 것).

### 쓸 수 있는 것 (준비 끝)

| | 위치 | 상태 |
|---|---|---|
| 활동사진 웹 165개 | `assets/images/activity/` (13MB, 추적 안 됨) | content 1600px · 55장 |
| 활동사진 원본 산출 | `clients/dakgong/assets/photos/국내캠프_활동_2026-08/` | hero·content·thumb + MANIFEST |
| 시설사진 | `assets/images/facility/` (12MB, 추적 안 됨) | 27장 |
| 인물 필터 프리셋 | `dakgong_domestic_people` | 도구에 등록 · 근거 주석 포함 |

**카테고리 7종** — lecture(8) · level-test(6) · mentoring(7) · daily-life(12) · class(4) ·
self-study(8) · consulting(10). 파일명 `dakgong-domestic-<슬러그>-NN-content.webp`.

### ★ 사진 필터가 시설/인물로 갈렸다 (2026-08-16 회장 승인)

기존 `dakgong_domestic` 은 **시설사진(벽·바닥)으로만** 검증한 값이었다. 인물 8컷으로 재니
피부 노란기가 원본 b\* 18.3 → 22.8(+4.45), 피부 ΔE 6.06 — **시설에서 탈락시킨 "강"(6.2) 수준**이었다.
원인은 Vibrance 하나(채도 0이면 b\* +0.83). 채도 .30→.15, 샤프닝 .45→.40 만 내린
`dakgong_domestic_people` 로 인물 55장을 뽑았다(피부 ΔE 4.42 = 승인된 "중" 자리).
**시설 27장은 재출력하지 않는다.** 갈리는 기준은 피사체이지 브랜드가 아니다.

### ★ 자리표시(.dk-ph)는 이미 다 짜여 있다 — 새로 만들 필요 없다

각 페이지에 `<div class="dk-ph">` 자리표시가 있고 **어떤 컷을 요청했는지까지** `cap`/`spec` 에 적혀 있다.
`_components.css` 에 `.dk-ph.is-filled > img` 가 이미 있어 **`is-filled` 클래스만 붙이고
안쪽을 `<img>` 로 갈면 끝**이다. CSS 도 락 파일도 안 건드려도 된다.

| 페이지 | 사진 자리 | 요청 컷 |
|---|---|---|
| curriculum | 6 | 수학 수업 · 중등부 · 고등부 · 개별 질문 · 교재와 필기 · 레벨테스트 현장 |
| self-study | 4 | 자습실 전경 · 담당자 순회 · **학습 플래너✗** · **오답 노트✗** |
| teachers | 1 (+프로필 4) | 멘토링 1:1 / **프로필 4칸은 회장 지시로 안 채운다** |
| life-care | 3 | 기상·점호 · 생활 담당 순회 · **세면✗** |
| consulting · index · contact | 0 | 자리 없음 |

✗ = **실물이 안 찍혔다.** 학습 플래너·오답 노트 단독 클로즈업, 세면 컷이 없다.
회장 결정으로 **그 자리는 비워 둔다** — 자리표시가 곧 다음 촬영 요청서다.
「기상·점호」에 넣을 사진도 실제로는 **아침 체조**다(점호 컷 없음). alt 에 "점호"를 쓰면 지어내기가 된다.

### ★★ PC 레이아웃이 깨지는 지점 — 여기서 하루를 썼다

**자리표시일 땐 안 보이고 실물 사진이 들어가야 드러난다.** 세 번 같은 구조로 당했다.

1. **`.dk-ph-split`** — 글칸 340×100 vs 사진 890×424. 글 옆에 **324px 빈 공간**.
   글은 2~3줄뿐이라 사진 높이를 절대 못 채운다. 사진을 줄이는 것도 답이 아니다
   (글 높이에 맞추려면 4:1 띠가 돼서 사진이 60% 잘림). 글칸을 좁히면 사진이 넓어져 **더 나빠진다**.
2. **`.dk-ph-tri`** — 큰 칸은 `aspect-ratio` 고정인데 작은 칸은 사진 원본 비율대로 자라
   큰 칸 아래가 **201px 빈다**(1440px). 자리표시는 `min-height:130px` 라 안 보였다.
3. **정렬** — 사진 블록만 **x=470** 에서 시작한다. 위 섹션들은 전부 **x=80**. 아무 것과도 안 맞는다.
   ★ 여백을 네 번 고쳐도 안 잡힌 진짜 이유가 이것이었다.

**시도했다 반려된 안** — 위정렬 / 글칸 확대 / 글칸을 옅은 면으로 / 제목·본문 벌리기 /
가로 띠 / 시설 갤러리 문법으로 통일. 회장 평: "다 이상한데", "별로네".
**형태는 회장이 정한다. 실행층이 안을 찍어내는 방식은 이번에 안 통했다 — 레퍼런스를 받아서 맞춘다.**

### ★ 사진 비율 — 극단 크롭 금지 (회장 반복 지적)

> "사진 가로세로 비율 저렇게 너무 길거나 너무 늘린 거 분명 쓰지 마라 했는데"

`object-fit: cover` 라 **칸 비율이 원본(1.50:1)에서 멀수록 잘린다.**
남는 비율 = `min(원본비/칸비, 칸비/원본비)`. 실측했던 값:

| 어디 | 칸 비율 | 사진 남는 비율 |
|---|---|---|
| `.dk-ph-split` 폰 | 0.77:1 | **51%** |
| `.dk-ph-split` PC | 2.11:1 | 71% |
| 시설 갤러리 벤토(당시) | 4.92:1 | **30%** |

⚠️ **시설 갤러리도 같은 문제였다.** 승인 시점엔 사진이 없었으므로
**"승인된 배열"이 곧 "승인된 비율"이 아니다.**

→ **검사 항목으로 박았다** (`verify/03_layout.py`): 사진이 **75% 미만 남으면 실패**.
   같이 고친 것 — 검사가 `loading="lazy"` 사진을 건너뛰던 구멍(스크롤로 전부 띄운 뒤 잰다).
   그 구멍 때문에 "통과"가 **"안 쟀음"** 이었다. 고치니 18건 → 35건으로 늘었다.

### 다음 세션(페이지별)이 할 일

1. **레퍼런스를 먼저 받는다.** 형태를 실행층이 고르지 않는다. 받으면 **요점을 문장으로 정리해 확인**받고 구현.
2. 사진은 `assets/images/activity/` 에 이미 있다. **다시 뽑을 필요 없다.**
3. 끝나면 반드시 `verify/all.py` — 특히 **사진 잘림 0건**을 확인한다.
4. 🔒 락 파일(`tokens-domestic` · `_shell` · `_components` · `_legacy`)을 고쳐야 하면 **멈추고 보고**.
5. **배포는 회장 승인 게이트.** 지금 미푸시 커밋 16개가 그대로 쌓여 있다.

### 정리 안 된 것

- `assets/images/activity/` 13MB · `assets/images/facility/` 12MB — **추적 안 되는 상태로 남아 있다.**
  쓸 거면 커밋, 안 쓸 거면 지운다. 방치하면 다음 사람이 뭔지 모른다.
- `_legacy.css` 삭제 여부 (이전 세션부터 보류)
- 8/4 시설사진 복원 여부 (위 백업 참조)

---

## 2026-08-03 세션 (3) — CSS 락/비락 분리 + 대비 정리 + 시설 갤러리

### ⚠️ 배포 대기 — 커밋 15개가 아직 안 올라가 있다 (`ahead 15`)

**라이브(`dakgong-domestic.netlify.app`)는 세션 시작 시점 그대로다.** 회장이 "배포는 나중에"로
보류했다. 이어받는 세션은 **회장 승인 없이 push 하지 않는다**(STATUS.md 1-1 배포 게이트).

### 파일 구조가 바뀌었다 — `site.css` 는 이제 없다

```
assets/css/  tokens-domestic.css  🔒 디자인 코어
             _shell.css           🔒 헤더·메뉴·푸터·히어로·롤링밴드·하단CTA·전역기본값
             _components.css      🔒 2페이지 이상 공용
             _legacy.css          ⚠️ 어디에도 안 쓰이는 규칙 격리(삭제 판단 보류)
             page-<페이지>.css    ✏️ 그 페이지 세션이 고치는 유일한 CSS
불러오는 순서: tokens → _shell → _components → _legacy → page-*
```

🔒 파일을 고쳐야 하면 **멈추고 회장께 보고**한다. 고친 뒤에는 `verify.config.json` 의
`lock_baseline` 을 새 커밋 해시로 갱신해야 자물쇠 검사가 다시 맞는다.

### 쪼개면서 세 번 깨졌다 — 같은 실수 반복 금지

전부 **순서가 뒤집혀서**다. CSS 는 같은 특성도면 뒤엣것이 이긴다.
1. `@media` 블록을 통째로 한 파일에 넣음 → 모바일 override 가 기본 규칙보다 먼저 로드돼 무력화
2. 변형 클래스(`X--Y`)를 기본(`X`)과 떼어놓음 → 눌려 있던 규칙이 되살아남
3. 쉼표로 묶인 규칙을 **가장 좁은 선택자** 기준으로 분류 → 나머지 페이지가 문단 기본값을 잃음
   → 쉼표 규칙은 **가장 넓은 선택자**가 소속을 정한다

### ★ 기계 검증이 통과해도 눈으로 봐라

색 이름표 15개를 `:root` **밖**에 넣어 `var(--dk-navy-deep)` 이 해석되지 않았고,
**히어로 그라디언트가 남색 → 회색으로 통째로 깨졌다**(7페이지 전부, 흰 글자가 묻힘).
그런데 검사는 **전부 통과**했다 — hex 를 var() 로 바꿔놨으니 색 검사에 안 걸리고,
snapshot 은 `backgroundColor` 만 재고 `backgroundImage` 를 안 쟀다.
회장이 "디자인이 이상해졌다"고 지적하고 스크린샷을 찍어보고서야 잡혔다.

> **통과했다는 말은 "잰 것 중에 문제가 없다"는 뜻이지 "문제가 없다"가 아니다.**

두 구멍은 도구에 항목으로 추가했다(`01_tokens` 미정의 var 검사 · `snapshot` 배경이미지).

### ★ 인포그래픽은 라이브 버전 유지 (회장 지시)

대비 4.5:1 을 맞추려고 `--dk-fg-muted` 를 어둡게 했더니 인포그래픽 라벨이 같이 바뀌었다.
**회장 지시로 원복.** 미달분은 `verify.config.json` 의 `contrast_exempt` 에 **사유와 함께** 기록했다
(사유 없으면 예외로 안 쳐준다). 검사 때마다 "예외 색으로 N곳 건너뜀"이 출력된다.

⚠️ 특히 `--tone` 사다리(01 코발트 → 02 `#2a2a2a` → 03 `#5c5c5c` → 04·05 `#8f8f8f`)는
**색약 명도비가 검증된 값**이다. "눈으로 구분 안 되니 통합"하면 안 된다 — 구분하라고 만든 단계다.
(`#5c5c5c` 를 `#555555` 로 합쳤다가 되돌렸고, `--dk-tone-3` 으로 등록했다)

### 화면이 실제로 바뀌는 것 (배포 시)

푸터 저작권(밝기·굵기) · 어두운 메뉴 링크 색 · 안내 상자 모서리(각짐) · **시설 갤러리 신규**.
나머지(구조 분리·색 이름표·인라인 여백)는 **1픽셀도 안 변한다** — 라이브 대비 실측
PC 0.012~0.102% / 모바일 0.033~0.361%, 7페이지 높이 전부 동일.

### 시설 갤러리 (생활환경 페이지) — 사진 대기 중

필핀 `facility.imweb.html` 의 01~05 벤토 골격 이식. 색만 코발트로 치환.
**사진이 아직 없다.** 자리·갯수만 잡혀 있고 실물이 들어오면 `img` 를 채운다.

| | 항목 | 확보 | 배열 |
|---|---|---|---|
| 01 | 캠프 전경 | 11장 | p0 + 「전체 보기」 버튼 |
| 02 | 강의실 | 4 | p2 |
| 03 | 강당 | 4 | p3 (가로 띠) |
| 04 | 숙소 | **3 — 1장 부족** | p4 (좌우 반전) |
| 05 | 식당 | 4 | p2 |

사진 원본: `C:\Users\ADMINY\Desktop\dakgong_result\<항목>\`
- **버튼은 01 에만.** 4장짜리는 4장이 다 보이므로 「전체 보기」가 거짓말이 된다(회장 지적).
- 배열을 섞은 이유: 다섯이 다 같으면 지겹다(회장 지적). 단 **바깥 상자는 468px 로 전부 동일**해
  스크롤 리듬은 유지된다. 변주에 근거를 둔다 — 사진이 많은 섹션만 버튼이 붙는다.
- 확대창은 `assets/js/gallery.js`. 사진·버튼 클릭 → ←/→ 넘김 · Esc 닫기.
- **불렛 문구는 지어내지 않았다.** 이 페이지 통계와 홈 FAQ(Q17·Q33·Q39·Q40)에 있는 것만 썼다.
  03 강당은 근거 있는 게 「강당 1개」 하나뿐이라 하나만 뒀다.

### 남은 것

1. **배포** (회장 승인 대기)
2. **숙소 사진 1장** 확보 → 빈 칸(빗금+점선) 채우기
3. **시설 사진 26장** 실물 삽입
4. `_legacy.css` 삭제 여부 판단
5. 디자인 코어 ↔ 사이트는 **동기화 완료**(토큰 21개 역수입). 오픈디자인 앱은 미동기 —
   닥공 웹에선 참고용이라 급하지 않다.

---

## 2026-07-28~29 세션 (2) — 표·인포그래픽 재설계 + 디자인 시스템 근본 수정

**최종 배포 `635c99c`. 미푸시 0건 — 로컬과 배포본이 완전히 같다.**

### ★ 포인트 컬러 사용 규칙 (2026-07-29 신설, 반드시 지킬 것)
회장 지적 "국내캠프가 필핀보다 포인트 컬러를 너무 대놓고 많이 써서 촌스럽다" → 실측하니
코발트를 **면(배경)에** 쓰는 요소가 페이지당 최대 **61개**였다. `site.css` 상단에 규칙을 주석으로 박아뒀다.

> **면에 코발트를 쓰는 곳은 5곳뿐이다.**
> ① 롤링 밴드(브랜드 띠) ② CTA 섹션 ③ 영상 재생 버튼 ④ 수학 pill ⑤ 월간 일정표 입소일
> 그 외 강조(배지·태그·스텝번호·표 헤더·콜아웃)는 **다크(`--dk-surface-dark`)나 중립 명도**로.
> 글자·아이콘 색으로서의 코발트는 이 규칙과 별개.

결과: index 11→6 · curriculum 27→2 · teachers 3→2 · self-study 6→2 · life-care 61→11
근거: `open_design/craft/anti-ai-slop.md` "var(--accent) 6회 이상 = P1 tell"

### ★ 다크 표면은 `--dk-surface-dark`(#2a2a2a) 하나뿐
`.dk-perf`와 teachers 선발기준이 `#1a1a1a`를 배경으로 쓰고 있었다. **`#1a1a1a`는 `--dk-ink`(글자색) 토큰이라
배경으로 쓰는 건 토큰 오용**이고, 하위 5페이지 중 teachers만 다른 다크 톤이라 "배경이 다채롭다"의 원인이었다.

### 색을 바꾸면 범례도 같이 바꿔라
pill 색을 바꾸면서 **범례 스와치가 옛 색을 가리키고 있었다**(9건). 범례는 마크와 1:1이어야 하고,
눈이 아니라 **계산값으로 대조**해야 잡힌다.

### 마지막 라운드(2026-07-29)에서 추가로 한 것
- 홈 브랜드 섹션 **순서 교체** — PHILOSOPHY 원칙 블록이 위, "닥공 Summer-Winter" 캠프 소개가 아래.
  구분선도 아래 블록의 `border-top`으로 이동(`.dk-brand-intro--after`). 안 옮기면 섹션 끝에 선이 남는다.
- **같은 섹션의 두 타이틀 크기 통일** — `.dk-brand-kicker`가 혼자 `clamp(22~30px)`를 쓰고 있어
  `.dk-h2`(36px)와 안 맞았다. → `var(--dk-display-lg)`로 통일, 구분은 색으로만.
  모바일 26px 강제 규칙에도 kicker를 함께 물림. 실측 1440px 36/45, 390px 26/32.5 양쪽 일치.
- **CTA `문의하기` 라벨 5개 페이지 전부 제거** — 버튼에 이미 상담/문의 단어가 있어 중복.

### 위키 반영 완료 (회장 승인)
`옵시디언 위키/wiki/persona/work/`에 2건 신규 — 후보 3건 중 앞 2건이 동일 원칙이라 통합.
- `규칙은-정본에-박아야-지켜진다.md` — 좋은 규칙도 로그·기억에 있으면 안 지켜진다.
  + 딸린 실행규율 "같은 유형 결함을 두 번째 발견하면 즉시 전수 점검"
- `브랜드-인포그래픽은-소구위계를-따른다.md` — 크기는 데이터 값이 아니라 소구 강도를 따른다
`index.md`·`log.md` 갱신 완료.

---

배포 이력(`57dfbda` 시점 기준 정리). 아래 4개가 이번 세션의 핵심이고, **전부 원인이 "규칙은 있는데 안 지켜지고 있었다"였다.**

**① 디자인 시스템 특성도 버그 (가장 중요)**
tokens의 `.dk p` / `.dk h1~h6`가 특성도 (0-1-1)이라 컴포넌트 클래스 (0-1-0)를 전부 이겨서
**41개 요소가 자기 값을 못 받고 16px/1.6 기본값으로 떨어져 있었다.**
(`.dk-perf-title` 32px→16px, `.site-footer__*` 4종, `.dk-label`, `.dk-kp-text`, `.dk-pt-h` 등)
→ 기본값 선언을 `:where(.dk) p` 형태로 감싸 특성도 0으로 낮춰 해결.
**앞으로 p·h 요소용 클래스를 만들 땐 이 함정을 먼저 의심할 것.**

**② 소구 위계 — 크기를 데이터값에 묶지 말 것**
막대 길이=인원수로 매핑하면 1,500이 무조건 제일 크고 SKY 250이 제일 작아진다.
허브 다이어그램(값 비교용 도형 아님)으로 바꿔 크기를 위계에 쓰게 함.
9시간도 24시간을 분모로 두면 무조건 1/3로 작아 보여서 → 252시간(9×28일) 절대량으로 전환.

**③ 수치 귀속** — `info.md` 확인: 5,000명·재참가 60%는 **필핀+국내 합산 20년 실적**이다.
국내캠프 페이지에서 라벨 없이 쓰면 오독된다. `닥공캠프 20년 누적` / `한국캠프 …`로 구분 표기함.
근사치 표시(`약`, `이상`)를 절대 떨어뜨리지 말 것.

**④ 줄바꿈·타이포 규칙** — 좋은 규칙이 프로젝트 로그에만 있고 작업기준엔 없었다.
`tools/web/web agent/CLAUDE.md`에 **「한글 타이포·줄바꿈 기준」**으로 정본화함.
히어로 분량을 index 기준(타이틀 15자/서브 25자)으로 통일, 서브 `<br>`은 820px 이하 제거
(`<br>` 앞 공백 필수 — 없으면 모바일에서 어절이 붙는다).

**⑤ 시각 콘텐츠 슬롯** — 4개 페이지가 이미지 0%였다. 슬롯 레이아웃 5종(밴드/좌우분할/
비대칭3분할/오프셋2장/균등4열)을 만들어 배치, 데스크톱 기준 40~54% 달성.
실물이 오면 `.is-filled` 클래스 붙이고 `<img>`만 끼우면 된다. 각 슬롯에 필요한 컷과 권장 비율 명시됨.

### 이 세션에서 신설된 지침 (반드시 읽을 것)
`tools/web/web agent/CLAUDE.md`
  - **「★ 디자인 코어 준수 — 0순위 체크리스트」** 5개 항목, 전부 기계 검증 가능
  - **「★ 한글 타이포·줄바꿈 기준」** 흩어져 있던 규칙 통합
`tools/web/web agent/AGENT.md` — 절대 순서에 **7. 디자인 프리뷰 승인** 단계 신설

### 회장 개인 사항
- **회장은 색약이다.** 색으로 구분시킬 땐 명도비를 계산으로 확보(인접 최소 1.9:1)하고
  반드시 텍스트 라벨을 병기할 것. 눈대중 판단 금지.

### 남은 것
- POINT 5 전용 페이지(대치동 컨설팅) — 자료는 `닥공\부산센터 브러셔.pdf`(메이저맵).
  착수 전 회장 확인 3건: 메이저맵과 닥공의 관계 / 컨설턴트 실명·사진 게재 가능 여부 / 가격표 게재 여부
- 일과표 교시 수 vs "하루 9시간" 대외표기 충돌 — 닥공 확인 대기.
  **life-care.html 손댈 때 회장께 먼저 리마인드할 것**
- 모바일 시각 슬롯 비중 24~30%(목표 33%) — 한글 텍스트·표가 세로로 길어진 탓. 더 올릴지 판단 대기
- 개인정보처리방침 시행일·개인정보보호책임자 성명·직책

---

## 2026-07-28 세션 (1)

닥공 1차 피드백 반영 + 슬라이드 메뉴 정합 + 일과표 실물 반영. 커밋 `fc3df51`, 배포 완료.

**한 것**
- 홈: 히어로 문구 교체 / 브랜드 섹션 도입부 2줄 신설 + 본문 교체 / "캠프 한눈에 보기" 표 신설 /
  연혁 2026 교체(부산경남 입시컨설팅 센터·해외대학유학 진학 컨설팅) / **4대→5대 핵심역량**
- 롤링밴드 5번째 문구를 5개 페이지 전부에 추가(contact엔 밴드 없음 — 정상)
- 슬라이드 메뉴: 패널 안 로고·닫기(×) 신설, 닫기 3경로(×/바깥클릭/Esc) 신설, 항목 가운데정렬
- 생활환경 일과표 [TODO] → 실물 3종(월간 예시·중등부·고등부)
- CSS 버그 2건 수정(아래 "함정" 참조)

**다음 세션이 이어받을 것**
1. **POINT 05 전용 페이지 제작** — 어바웃캠프 5번째 페이지. 자료 = `C:\Users\ADMINY\Desktop\닥공\부산센터 브러셔.pdf`
   (메이저맵 컨설팅 / MAJOR MAP). 만들 때 ①홈 POINT 05 `href`(현재 `#`) ②6개 파일 헤더·패널 드롭다운
   ③`README.md`를 같이 고칠 것.
   - **착수 전 회장 확인 필수**: 메이저맵은 닥공과 로고·주소·메일이 전부 다른 별개 브랜드다(제휴/입점/계열 미확인).
     브로셔엔 컨설턴트 6인 실명·얼굴사진과 가격표 10종이 있어 게재 가능 여부를 먼저 물어야 한다.
2. **레이아웃 표현 강화(2단계, 회장 답변 대기 중)** — 표/영상자리/POINT시각/인포그래픽 4종 컴포넌트 개선안까지
   보고했고, 그중 "사선 섹션 경계 + 대형 아웃라인 워드마크"는 필핀에 없는 구조라 원칙을 푸는 결정이 필요해 보류 중.
3. 아래 "미확인" 2건(개인정보처리방침 시행일·책임자)은 여전히 미해소.

---

## 지금 상태 (요약)

- 6페이지 정적 사이트, 실제 배포됨: https://dakgong-domestic.netlify.app
- 최신 배포 커밋 `7ce5177` — 정상 반영 확인됨(넷틀리파이 API + curl 직접 확인).
- **1차 시안으로 고객(닥공) 보고 예정 단계.** 이번 세션은 그 직전 마무리 작업이었음.

## 이번 세션에서 한 일 (커밋 순서대로)

1. 상단메뉴 밑줄 제거, 모바일 히어로 서브카피 줄바꿈/폰트 수정
2. 모바일 메뉴 전면 재작성 — 필핀 실측(전체화면 흰 오버레이 → 70%폭 다크 슬라이드 패널)
3. 상단메뉴 밑줄 시스템 완전 삭제(회장이 시킨 적 없는 임의 추가분 제거 — 교훈: 지시 없는 UI 디테일 임의 추가하지 말 것)
4. 연혁 필핀 실제 컴포넌트 이식, em-dash 전량 제거, 히어로 서브카피 축약, POINT섹션 정렬, FAQ 헤더 중앙정렬
5. 홈 브랜드소개 섹션 — 필핀식 3단 감성 스토리텔링 제거 → 닥공이 준 직설적 원문으로 교체
6. FAQ 카테고리라벨/제목/목록 폭 불일치 해결, 이중박스 대비 개선, 영상인터뷰 캐러셀→고정3칸 복원
7. **헤더 메뉴 구조 전면 재작성**(`def2fbf`) — `.site-nav-bar`(PC 가로메뉴, 항상 보임) + `.site-nav`(삼선이 여는 다크 슬라이드 패널, 전 폭 공통)로 완전 분리. 이전엔 PC에서 삼선 눌러도 반응 없었음(버그). 이 커밋에 contact.html 개인정보처리방침 전면 교체도 같이 들어감.
8. **`cf634a9`** — 홈 1~3섹션(숫자/연혁/영상인터뷰) 모바일 디테일:
   - 연혁(`.dk-history`) 모바일 CSS 4개 누락분 발견·수정: 래퍼 좌우패딩, 연도숫자 26→19px, 본문 15.5→14px, 점 13→11px. (필핀 원본엔 있는데 우리 site.css에 이 4줄이 통째로 빠져있던 실제 버그였음 — "차이 없다"고 잘못 판단했던 사례, 코드 diff로 검증해서 찾음)
   - 숫자(TRACK RECORD) 섹션 → 다크배경 + 원형 아이콘뱃지 + 숫자 + 설명문 레이아웃(`.dk-stat-dark-*`)으로 전환. 기존 밝은 `.dk-g4`/`.dk-stat4`는 그대로 남겨둠(life-care.html 시설 통계가 아직 씀 — 절대 같이 건드리지 말 것, 용도 다름)
   - 영상인터뷰(`.dk-vi-card`) → 재생버튼 중앙 큰 배지에서 **우상단 작은 원형 배지**로, 캡션은 카드 중앙에서 **좌하단 반투명 오버레이 바**로 재배치. PC 3칸 그리드 / 880px 이하 모바일 1칸 수직. (필핀 실사이트는 아임웹 기본 `gallery2` 위젯이라 정확한 재생버튼 px값은 못 뽑음 — 위치·구성만 재현)

## 2026-07-30 세션 — PC/모바일 레이아웃 정리 + 인포그래픽 4차 반복

**배포 완료** `d3bd163` (커밋 3개 묶음: `1f5a567` 핸드오프 · `52c0416` PC 레이아웃 · `d3bd163` 여백 스케일)
라이브 검증: tokens/site.css에 `--dk-gap-*` 반영 확인, 6페이지 전부 200, contact `dk-ph` 잔여 0.

### 코드로 반영된 것 (배포됨)

1. **섹션 내부 여백 스케일화** — 회장 지적 "섹션내에서 상하여백 규칙 개판".
   실측 결과 섹션 내부 간격이 **0~46px 사이 16종**. 원인 두 개:
   ① 역할이 같은데 clamp 범위만 다른 CSS 선언 10종 ② HTML 인라인 margin 20개.
   → tokens에 **5단 스케일**(`--dk-gap-xs/sm/md/lg/xl`) 신설, CSS 26곳 + 인라인 22곳 치환.
   **결과 16종 → 5종.** 모바일 12/16/26/40/56 · PC 12/20/34/56/80.
2. **PC 레이아웃** — 슬롯 블록이 `<section>` 없이 `.dk-wrap`만 걸쳐 섹션 리듬을 못 받고 있었다
   (간격 0/20/84px 제각각). `.dk-ph-block` 신설해 15곳 적용. 최대 블록 높이 836→704px.
3. **모바일 슬롯 배치 버그 3건** — `.dk-ph-tri.rev` 계단식 어긋남 / `.dk-ph-split.rev` 이미지 890x72 눌림 /
   `.dk-vi-grid` 1열에서 2px 붕괴. 상세는 아래 "함정" 절.
4. **CTA 버튼 폭 통일** — '상담 신청'(4자)과 '카카오톡 상담'(6자)이 auto 폭이라 달랐다.
   `inline-grid` + `grid-auto-columns:1fr`. 전 페이지 144x44 동일, 480px 이하는 1열 320px.
5. **contact 시각 슬롯 제거** (회장 지시). `dk-ph` 잔여 0.
6. **1280px 가로 스크롤 6px** (기존 결함) — `[open]` 상태 `+` 아이콘의 45도 회전이 레이아웃 폭에
   반영되지 않아 바운딩 박스만 10px 커짐. 1280px 이상에선 `.dk-wrap` 패딩이 0이라 그대로 스크롤이 됨.
   아이콘을 24px 정사각으로 고정해 해결.

### 코드 반영·배포 완료 (2차 배포분)

**최종 배포 `3e52724`** — 커밋 11개. 7페이지 전부 200 확인.
`f2ed734` 컨설팅 페이지 신설 + 벌집 인포그래픽 + PC 가운데정렬 버그
`fd20ca0` 회장 지적 4건 · `89c53c7` POINT 좌우교차 · `a91ca29` 별도 서비스 성격 ·
`75030d1` 원문 밖 정보 6건 제거 · `74bed9c` 모바일 벌집 복구

1. **커리큘럼 「6대 핵심 수업 구성」** — 아코디언(`dk-course-list`) 폐기 →
   **벌집(육각형) 인포그래픽 + 상세 6칸 통일 템플릿**. ★현재 배포된 최종 형태다.
   · 중앙 육각형에 섹션 제목, 둘레 6개에 6대 핵심(시계방향 원문 순서). 각 육각형의 컬러 쐐기를
     **중앙 보는 모서리로 몰아** 6개가 모여 중앙을 감싸는 육각형 링이 된다. 아이콘은 쐐기 안,
     텍스트는 흰 부분. 구현 = 중심 기준 회전 반평면(`--ang` 하나로 6방향).
   · 쐐기 색 = 운영 방식(코발트 수학 / `#2a2a2a` / `#5c5c5c` / `#8f8f8f`). 링 이웃 명도비
     2.07~2.15:1(색약 기준 1.9 초과), 흰 아이콘 대비 최저 3.23:1, 범례에 글자 병기.
   · **모바일(≤880px)**: 육각형 구조를 유지하고 육각형 폭을 `(100vw-24px)/3`로 잡는다.
     칸 안에는 **번호 + 제목**만 남기고(라벨·설명·아이콘 감춤) 제목의 **뒷부분은 `.dk-hx__tl`로 감춘다** —
     지어낸 축약이 아니라 원문 단어를 자른 것: `국어 · 영어(특강)` / `수능 대비(집중반)` /
     `과학 · 사회(선택과목)` / `SKY · 의대(선배 멘토링)` / `(관리형) 자기주도학습`.
     실측 320~880px에서 **육각형 사선 밖으로 나간 칸 0건**, 제목 전부 1줄(11~14px).
   · 벌집 배경은 **`.dk-wrap` 밖(섹션 직계)** 에 둬서 풀블리드.
   · 상세 6칸 = 하나의 템플릿(상단 3px 층색 실선 + 번호·아이콘·제목·라벨 + 구분/내용 3행).
   · ⚠️ **한 번 아크+알약형 구조로 갈아엎었다가 회장 지시로 되돌렸다**(`afd323c` → `3e52724`).
     회장: *"구리다…걍 육각형 버전으로 되돌려라"*. **벌집이 확정 형태다. 다시 바꾸지 말 것.**
     (아크 구조를 다시 제안하지 않는다. 모바일 제약은 위 방식으로 해결돼 있다.)
2. **입시컨설팅 페이지 신설 `consulting.html`** — 아래 "입시컨설팅" 절 참조.
3. **메뉴** — ABOUT CAMP 하위 6번째 `입시 컨설팅` (6페이지 × PC바/슬라이드 = 12곳).
   기획안 v2의 "메뉴 5칸" 판단은 자료가 없어 페이지를 안 열기로 했던 것이므로 회장 승인으로 뒤집었다.
4. **index POINT5** `자세히 보기` 빈 링크(`#`) → `consulting.html`.
5. **POINT 좌우 교차 복구** — `fc3df51`(4대→5대 확장)에서 POINT05를 POINT04에서 복사하며
   `dk-pt-item--rev`를 지우지 않아 04·05가 연달아 좌측이었다. 01 우 / 02 좌 / 03 우 / 04 좌 / 05 우로 복구.

### ★입시컨설팅 페이지 — 확정 사항 (닥공 통화 + 회장 지시)

- **성격**: 캠프 비용에 **포함 아님. 별도 유료 서비스.** 단 **유료라는 사실을 페이지에 노출하지 않는다.**
  POINT5에 두는 이유 = 캠프 전문성 제시 + **간보기**. "닥공의 전문 비즈니스 영역 중 하나"로 쓴다.
  별도 절차임은 `학생 개별` · `신청` 같은 단어로만 읽히게 한다.
- **명시 문구**(회장이 직접 준 문장, 01 WHY 아래 중간 여백에 12.5px 회색으로):
  `(해당 서비스는 캠프 이후 별도 신청자 한정으로 진행되는 서비스로 개별 문의 부탁드립니다.)`
- **CTA 층위 분리** — 컨설팅 문의(`중등 입시 컨설팅 문의` / `고등 입시 컨설팅 문의`)는
  **03 PROCESS 직후 본문 안 흰 카드**, 캠프 문의는 **페이지 끝 풀블리드 코발트**(6페이지 공통).
  하단 공통 CTA를 컨설팅으로 교체하는 안은 채택하지 않았다 — ①6페이지 공통 CTA가 깨짐
  ②캠프 문의 경로 소실 ③유료 서비스를 결론 자리에 놓으면 판매 페이지가 되어 "간보기" 성격과 어긋남.
- **자료 원본**: `C:\Users\ADMINY\Desktop\닥공\부산센터 브러셔.pdf` (6장, 전부 이미지형 → PyMuPDF 렌더 후 판독)
  · 쓴 것 = p2 학생관리시스템 3축 · p4 프로세스 4단계+2 · p5 준비 서류
  · 뺀 것 = 메이저맵 브랜드·로고·주소·이메일·QR · 컨설턴트 6인 실명·사진 ·
    상품 10종 가격표(30만~770만) · 접수 퍼널 · p3 진학실적(메이저맵 실적이라 닥공 실적으로 오독)
  · **계약 완료 시 메이저맵 브랜드를 05 SYSTEM 자리에 한 덩어리 추가**하면 됨. 구조 변경 불필요.
- 구성: 히어로 → 01 WHY → 02 SYSTEM(3축 수렴 인포그래픽 + 상세 3칸) → 03 PROCESS(4단계+2) +
  컨설팅 문의 → 04 PREPARE → 공통 캠프 CTA

## 알아야 할 함정 (Gotcha)

- **git push는 매번 회장 명시 승인 필요.** ("배포해줘"/"반영해" 등 명확한 말이 있을 때만 push. 로컬 커밋은 자유.)
- **승인이 있어도, 곧 추가 수정이 생길 것 같으면 push 직전에 한 번 더 물을 것.**
  특히 ①내가 올린 확인 요청 답변이 아직 안 왔거나 ②후속 작업이 바로 이어질 때.
  "지금 나갈까요, 확인 받고 묶어서 나갈까요?" — 크레딧은 배포 **횟수** 단위라 모아서 한 번이 항상 유리하다.
  (2026-07-28 회장 지적: 확인 6건 띄워둔 채 배포해서 2회 배포가 될 뻔함.)
- **넷틀리파이가 최근 크레딧제로 바뀜**(빌드분 아님). 무료 300크레딧/월, 배포 1회=15크레딧. 2026-07-26에 무료 크레딧 소진으로 배포가 안 걸리는 사고 있었음 → 회장이 Personal $9/월(1,000크레딧) 업그레이드로 해결. **크레딧은 "배포 횟수" 단위로만 소모되고 변경 파일 개수와 무관** — 수정 여러 개 모아서 한 번에 push하는 게 유리.
- **배포 확인은 브라우저 도구 대신 이 방법이 확실함**(이 환경에서 브라우저 도구가 netlify.app/localhost 접근이 막혀있음):
  - `curl -s https://dakgong-domestic.netlify.app/assets/css/site.css`로 실제 배포된 코드 직접 확인
  - Netlify MCP: `netlify-project-services-reader`(`get-project`, siteId=`28e27695-2535-4e21-889a-54c68eae7d3b`)로 `currentDeploy.id` 확인 → `netlify-deploy-services-reader`(`get-deploy-for-site`)로 `commit_ref`가 최신 커밋인지 대조
  - git push 후 자동배포가 안 걸리면(크레딧 등 이슈) `git commit --allow-empty` + push로 재트리거 가능. **`netlify-deploy-services-updater`의 `deploy-site`(CLI zip 업로드 방식)는 이 git-연동 사이트에서 계속 400 Bad Request로 실패함 — git push 방식만 쓸 것, CLI zip-deploy는 이 사이트에서 안 됨.**
- **"차이 없다"고 말하기 전에 반드시 코드 diff로 확인.** 스크린샷만 보고 판단하면 실제 CSS 누락(연혁 모바일 사례)을 놓친다. 필핀 원본은 `C:\Users\ADMINY\Desktop\claude code\clients\dakgong\imweb\_reference\home-live-source.html`(curl로 떠둔 실사이트 원본).
- 필핀 실사이트 IP를 색상만 바꿔 재사용하는 건 같은 법인(HMJ) 소유물이라 승인된 방식임(제3자 저작물 아님). 단 필핀 "리얼 피드백" 카톡 캡처 후기 7건은 **게재 금지 확정**(2026-07-17 회장 판정) — 절대 재사용하지 말 것.
- 사실·통계·후기 지어내기 절대 금지. 불확실하면 `[준영확인]` 표시.
- **CSS: p 요소용 클래스는 반드시 `.dk` 를 앞에 붙일 것.** `tokens-domestic.css`의 `.dk p`가 특성도 0-1-1이라
  클래스 하나짜리(0-1-0) 규칙을 이겨서 그냥 쓰면 조용히 안 먹는다(2026-07-28에 `.dk-table-note`가
  만들어진 이래 한 번도 적용된 적 없었다는 걸 발견). 기존 관례 = `.dk .lead`.
- **CSS: 미디어쿼리는 특성도를 안 올린다.** 파일 앞쪽 `@media(min-width:1280px){...}`에 넣은 규칙이라도
  그 뒤에 같은 선택자를 다시 선언하면 뒤엣것이 이긴다(`.dk-pt-in` 폭 어긋남의 실제 원인이었음).

- **여백도 토큰이다.** 색을 토큰에서만 고르듯 여백도 `--dk-gap-xs/sm/md/lg/xl` 5단에서만 고른다.
  `clamp()`를 매번 새로 쓰는 게 가장 흔한 오염 경로다(역할이 같은데 범위만 다른 선언이 쌓인다).
  **HTML 인라인 `style="margin-..."`은 금지** — 여기가 잡값의 주 통로였다.
- **여백을 측정할 땐 모션을 끈다.** `data-reveal` 트랜지션이 0.7s라 그 중에 재면 ±4px 잔상이 섞여
  잡값 5종이 12종으로 보인다. Playwright `reduced_motion='reduce'`로 재야 정확하다.
- **반응형에서 PC 배치를 되돌릴 땐 "부분 초기화" 금지.** 같은 요소에 건 `grid-column`/`grid-row`/`height`를
  **전부 한 번에** 초기화하고 새로 배치한다. 그리고 **초기화 선택자의 특성도를 원본과 맞춰야 한다** —
  PC 규칙이 `.a.rev > *:nth-child(2)`(0,3,0)인데 리셋을 `.a.rev > *`(0,2,0)로 쓰면 조용히 무시된다.
  `> *:nth-child(n)`으로 맞출 것. (모바일 tri가 계단식으로 어긋난 실제 원인)
- **`order`는 순서만 바꾸고 컬럼 폭은 안 바꾼다.** 좌우 반전(`.rev`)을 만들 땐 `grid-template-columns`도
  함께 뒤집어야 한다. 안 하면 이미지가 캡션 폭(340px)에 들어가 890x72로 납작해진다.
- **`transform`은 레이아웃 폭에 반영되지 않는다.** 45도 회전 아이콘은 바운딩 박스만 커져 컨테이너 밖으로
  나간다. 회전시킬 요소는 **고정 정사각 박스**로 묶고 여유 패딩을 준다.
- **비율 상한을 `max-height`로 걸지 말 것.** `aspect-ratio`가 걸린 칸은 높이를 누르면 폭도 같이 줄어
  컬럼보다 좁아지고 옆에 죽은 공간이 생긴다(1440px에서 780px 컬럼에 카드가 698px). → **더 완만한 비율**로 바꾼다.
- **`clip-path`를 가진 요소에 `filter`를 같이 걸지 말 것.** 자식(쐐기 등)이 클립을 못 받고 밖으로 나간다.
  **그림자는 부모에** 걸고 clip은 자식에 둔다.
- **★히어로는 절대 새로 만들지 않는다.** 실사이트는 홈과 하위 5페이지가 **완전히 같은 컴포넌트**
  (`.dk-bg-video-section` + `.dk-bg-video-placeholder` + `.dk-bg-video-content` 안의 `h1` + `p.lead`)를 재사용한다.
  이건 2026-07-25에 회장 지적으로 통일해 둔 것이다. **브레드크럼·라벨·킥커는 실사이트 히어로에 0건** —
  넣으면 안 된다. 값: min-height 600(768px 이하 520) · 패딩 80px 0(768px 이하 72px 24px) · max-width 1280 ·
  h1 Paperlogy 30px/700 ls-0.3(768px 이하 22px) · lead Pretendard 18px/400(768px 이하 16px/1.6).
  (2026-07-30 회장 지적: "히어로 섹션 디자인 또 지맘대로 바꿔놨네, 다른페이지들 하고 통일안하고")
- **★카드를 버리지 말 것 — "카드만" 쓰는 게 문제였다.** `site.css` 실측: radius-lg(16px) 13회 · md 8회 ·
  full 9회 · box-shadow 16회. **라운드 카드가 이 사이트의 기본 언어다.** 순수 편집형(라운드 0·상자 0)으로
  밀면 나머지 5페이지와 결이 깨진다(2026-07-30 회장 지적). 남는 금지는 하나뿐 —
  **라운드 + 컬러 좌측 레일 동시 사용**(`anti-ai-slop` P0 #5). 강조는 **상단 실선**으로.
  ⚠️ 실사이트 `.dk-callout`이 지금 이 금지를 위반 중(라운드 + `border-left:4px`) — 정리 대상.
- **★병렬 항목은 레이아웃을 통일한다.** 같은 종류의 항목 N개(6대 핵심 등)를 항목마다 다른 구조로 짜면
  병렬성이 깨져 세트로 안 읽힌다. **다양성은 페이지의 서로 다른 섹션 사이**에 두는 것이고 병렬 세트 안이 아니다.
  (2026-07-30 회장 지적: "6대 수업의 상세내용들 섹션 디자인 레이아웃은 통일시켜야 맥락이 맞다")
- **레퍼런스를 받으면 "요점"을 먼저 말로 정리할 것.** 벌집 레퍼런스를 3번 틀렸다 —
  육각형을 늘어놓기만 하고 ①중앙에 제목 ②쐐기를 중앙 보는 모서리로 몰아 링을 만드는 것
  ③아이콘은 쐐기 안, 텍스트는 흰 부분 이라는 요점을 놓쳤다.
- **★자료를 못 찾겠으면 저장소 밖도 볼 것.** 컨설팅 자료가 `Desktop\닥공\` 폴더에 있었는데
  `clients/dakgong/`만 뒤지고 "자료가 없다"를 반복했다. 회장이 준 첨부는 대화 기록
  (`~/.claude/projects/.../<session>.jsonl`)에서 파일 경로째로 검색할 수 있다.
- **★회장이 준 카톡·메모는 "지시"가 들어 있다고 보고 두 번 읽을 것.** 컨설팅 카톡에서
  이사님이 "메이저맵은 빼고 작업" = 회장이 제시한 두 갈래 중 **"닥공이 직접 제공하는 것처럼
  내용만 발췌해서 활용"을 승인**한 것이었는데, 나는 "제외"만 읽고 **"내용은 쓴다"를 놓쳤다.**

- **★★인포그래픽 구조를 고르기 전에 가장 좁은 폭(320px)에서 칸 폭을 먼저 계산한다.**
  벌집은 3열을 강제해 390px에서 칸이 114px다. 원문 제목을 그대로 넣으면 사선 밖으로 나간다.
  ⚠️ 그래서 아크+알약형으로 갈아엎어 봤지만 **회장이 "구리다"며 벌집으로 되돌리라 했다.**
  → **형태는 회장이 정한다. 실행층은 제약을 계산해서 "이 형태에서 이만큼만 들어간다"를 보고하고,
  담는 내용을 줄이는 방식으로 맞춘다.** 구조를 임의로 갈아엎지 않는다.
  벌집의 해법 = 번호+제목만 남기고 제목 뒷부분을 감추기(원문 단어를 자름).
  · 칸 폭 = (100vw − 좌우패딩) ÷ 열수. 한글 제목 1줄은 **칸 폭 ÷ 글자수 ≥ 12px**이어야 읽힌다.
  · 이 계산이 안 맞으면 그 구조는 쓰지 않는다. 텍스트를 지우는 걸로 해결하지 않는다.
- **★단색 톤 변주는 4단이 안 나온다.** 같은 hue로 인접 명도비 1.9:1을 4단 확보하려면
  L이 0.03/0.10/0.24/0.50까지 벌어져야 해서 브랜드색 범위를 벗어난다.
  **2단 교대(진/연) + 나머지는 라벨 글자로** 구분하는 게 색약에도 안전하고 브랜드도 안 깨진다.
- **★`@media` 안에 선택자 없이 선언을 쓰면 그 블록 파싱이 깨진다.** `--cut: 63%;`를
  `@media{}` 직계에 써서 뒤따르는 `display:none` 규칙이 무시됐고, 숨겼어야 할 텍스트가 나와 넘쳤다.
  CSS를 스크립트로 조립할 때 특히 쉽게 생긴다 — **전 파일 정규식 검사로 잡을 수 있다.**
- **★★모바일이 기준이다. 모바일부터 짜고 PC로 넓힌다.** 이번에 벌집 인포그래픽을 PC 전용으로 짜고
  ≤880px에서 `clip-path`를 없애 **평범한 카드 목록으로 떨어뜨렸다** — 모바일에서 인포그래픽이 통째로 사라졌다.
  회장: *"항상 모바일이 메인인데 왜 니맘대로 pc에만 적용한거야"*.
  **좁은 폭에서 안 들어가면 형태를 버리는 게 아니라 담는 내용을 줄인다.**
  벌집은 육각형 폭을 `(100vw - 24px)/3`로 잡고 둘레 칸을 [번호+아이콘]만 남겨서 360px까지 살렸다
  (텍스트는 바로 아래 상세 6칸이 담당, 번호 01~06이 1:1로 이어짐).
  ⚠️ **검사 항목에 "좁은 폭에서 형태가 유지되나"를 넣는다** — `clip-path`가 `polygon`으로 남아 있는지 등.
- **★공통 요소는 타이핑하지 말고 파일에서 복사한다.** 이번 세션에서 같은 실수를 **두 번** 했다 —
  ①히어로를 `.chero`로 새로 만들었고 ②롤링밴드 5개 문구를 새로 써서 컨설팅만 달랐다
  (`하루 9시간 이상 압도적인 수학 학습량` → `수학 중심 하루 9시간 이상 학습량`으로 변형).
  **CSS도 문구도 기존 파일에서 그대로 떠 온다.** 검증은 6페이지 해시 비교로 한다.
- **★`margin: 0`으로 `margin: 0 auto`를 죽이지 마라.** `.dk-ph-block`에 상하 여백을 주려고
  `margin: 0`을 썼는데 `.dk-wrap`의 가운데 정렬이 함께 죽어 1280px 넘는 화면에서 슬롯이 좌측에 붙었다
  (1900px 실측: `.dk-wrap` 좌310/우310 vs 슬롯 좌0/우620). 상하만 필요하면 **`margin-block`** 을 쓴다.
- **★검사 항목에 "가로 위치(좌우 여백 대칭)"를 반드시 넣는다.** 간격·가로스크롤만 재면 위 버그를 못 잡는다.
  `.dk-wrap`/`.dk-pt-in`의 `left`와 `innerWidth-right`를 비교해 4px 초과면 실패로 본다.
- **★검사 항목에 "글자색 = 배경색"을 넣는다.** `.dk a`(0-1-1)가 `.dk-cinq__b`(0-1-0)를 이겨
  코발트 글자 + 코발트 배경으로 **버튼 글자가 완전히 안 보였다.** `p`만 조심하면 되는 게 아니라
  **`a`도 같다** — 링크성 클래스는 `.dk` 접두를 붙여 0-2-0으로 올린다.
- **★풀블리드는 음수 마진으로 만들지 말고 `.dk-wrap` 밖으로 뺀다.** ≥1280px에서 `.dk-wrap` 패딩이 0이라
  파고들 여백이 없어 그대로 문서 밖으로 나간다(실측 1280px 가로 48px).
- **★다크섹션 전용 클래스를 밝은 섹션에 쓰지 마라.** `.dk-kp-note`는 `text-align:center` + 흰 글자다.
  밝은 섹션에 쓰면서 인라인으로 색만 덮어쓴 게 캡션이 가운데 정렬로 뜬 원인이었다.
  **인라인 색 override가 보이면 클래스 오용 신호다.** → `.dk-ph-note` / `.dk-table-note`로.
- **★"지어낸 정보 없나"를 스스로 먼저 검사한다.** 이번에 회장이 물어서야 **6건**을 잡았다 —
  롤링밴드 변형 · `전 학년`(원문에 대상 표기 없음) · `반드시`(원문 "필요한") · `최근 응시한`(조건 없음) ·
  `학년에 따라 설계가 달라지므로`(근거 없음) · `10년 이상`(메이저맵 연수).
  **검사법**: 페이지 본문 문구를 전부 뽑아 원문 조각과 1:1 대조하고, **조건·강도·연수·수량**을
  붙이지 않았는지 본다(`반드시`·`최근`·`전`·`10년` 같은 말이 원문에 있었는지).
- **★블록을 복제할 때 상태 클래스를 지운다.** POINT05를 POINT04에서 복사하며 `dk-pt-item--rev`를
  안 지워 좌우 교차가 깨진 채 여러 세션을 갔다. `--rev`·`--active`·`open` 같은 상태 클래스는 복제 후 점검.

## 미확인 / 회장 답변 대기 중

- `contact.html` 개인정보처리방침 제13조 **시행일** — 현재 `[준영확인: 시행일]` placeholder
- 개인정보처리방침 **개인정보보호책임자 성명·직책** — 현재 `[준영확인]` placeholder
- 이번에 배포한 시안(커리큘럼 벌집 + 컨설팅 페이지)에 대한 닥공 측 피드백
- **컨설팅 브러셔 p3 진학 실적(연세·서울대·의예과 등)** — 실행층 판단으로 제외 상태 유지.
  메이저맵 실적이라 브랜드를 지운 채 실으면 닥공 실적으로 읽힌다. (p2의 "10년 이상"도 같은 이유로
  회장 판단에 따라 연수만 제거함 — 2026-07-30)
- **메이저맵 정식 계약 여부** — 계약되면 브랜드를 컨설팅 페이지 05 SYSTEM 자리에 추가.
- 컨설팅 CTA 링크가 현재 둘 다 `contact.html`이다. **중등/고등을 분리 접수**하려면
  별도 폼 또는 문의 유형 파라미터가 필요하다(닥공 확인 필요).

## 다음 세션 첫 행동

0. **`tools/web/web agent/CLAUDE.md`의 「★디자인 코어 준수 0순위 체크리스트」와
   「★한글 타이포·줄바꿈 기준」을 먼저 읽는다.** 2026-07-28에 신설됐고, 이걸 안 읽으면
   같은 실수(색 지어내기·특성도 함정·줄바꿈 방치)를 그대로 반복하게 된다.
1. 회장이 닥공 피드백을 전달했는지 먼저 확인 — 있으면 그것부터 처리.
2. **커리큘럼 벌집 + 컨설팅 페이지는 이미 반영·배포 완료**(`75030d1`). 다음은 닥공 피드백 처리다.
   컨설팅의 성격 규정은 위 "★입시컨설팅 페이지" 절을 먼저 읽을 것 —
   **캠프 비용 미포함 별도 유료 서비스지만 유료라고 쓰지 않는다.** 접수 퍼널·가격표 금지.
   회장이 준 명시 문구는 그대로 유지한다(문장 임의 수정 금지).
3. **`life-care.html`을 손대면** 일과표 교시 수 vs "하루 9시간" 대외표기 충돌 건을 **먼저 리마인드**할 것(닥공 확인 대기).
4. 없으면 "미확인" 두 항목(개인정보처리방침 시행일·개인정보보호책임자) 여쭤서 contact.html 마무리.
5. 새로운 디자인 디테일 지적이 들어오면, 먼저 필핀 `home-live-source.html` 실제 코드에 해당 컴포넌트가 있는지
   grep으로 확인하고(있으면 그대로 이식, 없으면 아임웹 기본위젯일 수 있음을 먼저 확인) 나서 손댈 것 — 추측성 판단 금지.
6. **눈으로 봐야 판단되는 변경은 구현 전에 프리뷰부터.** (`AGENT.md` 절대순서 7단계 — Artifact로 발행해 링크로 승인)
