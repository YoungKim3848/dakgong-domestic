# 웹에이전트 핸드오프 — 닥공캠프 국내캠프 사이트

새 세션에서 이 프로젝트를 이어갈 때 반드시 먼저 읽을 것. `README.md`(구조)와 함께 본다.

---

## 2026-07-28~29 세션 (2) — 표·인포그래픽 재설계 + 디자인 시스템 근본 수정

**최종 배포 `635c99c`. 미푸시 0건 — 로컬과 배포본이 완전히 같다.**

### ★ 포인트 컬러 사용 규칙 (2026-07-29 신설, 반드시 지킬 것)
회장 지적 "국내캠프가 필핀보다 포인트 컬러를 너무 대놓고 많이 써서 촌스럽다" → 실측하니
코발트를 **면(배경)에** 쓰는 요소가 페이지당 최대 **61개**였다. `site.css` 상단에 규칙을 주석으로 박아뒀다.

> **면에 코발트를 쓰는 곳은 5곳뿐이다.**
> ① 롤링 밴드(브랜드 띠) ② CTA 섹션 ③ 영상 재생 버튼 ④ 수학 pill ⑤ 월간 일정표 입소일
> 그 외 강조(배지·태그·스텝번호·표 헤더·콜아웃)는 **다크(`--dk-surface-dark`)나 중립 명도**로.
> 글자·아이콘 색으로서의 코발트는 이 규칙과 별개.

결과: index 11→6 · curriculum 27→2 · teachers 3→2 · self-study 6→2 · life-care 61→11
근거: `open_design/craft/anti-ai-slop.md` "var(--accent) 6회 이상 = P1 tell"

### ★ 다크 표면은 `--dk-surface-dark`(#2a2a2a) 하나뿐
`.dk-perf`와 teachers 선발기준이 `#1a1a1a`를 배경으로 쓰고 있었다. **`#1a1a1a`는 `--dk-ink`(글자색) 토큰이라
배경으로 쓰는 건 토큰 오용**이고, 하위 5페이지 중 teachers만 다른 다크 톤이라 "배경이 다채롭다"의 원인이었다.

### 색을 바꾸면 범례도 같이 바꿔라
pill 색을 바꾸면서 **범례 스와치가 옛 색을 가리키고 있었다**(9건). 범례는 마크와 1:1이어야 하고,
눈이 아니라 **계산값으로 대조**해야 잡힌다.

### 마지막 라운드(2026-07-29)에서 추가로 한 것
- 홈 브랜드 섹션 **순서 교체** — PHILOSOPHY 원칙 블록이 위, "닥공 Summer-Winter" 캠프 소개가 아래.
  구분선도 아래 블록의 `border-top`으로 이동(`.dk-brand-intro--after`). 안 옮기면 섹션 끝에 선이 남는다.
- **같은 섹션의 두 타이틀 크기 통일** — `.dk-brand-kicker`가 혼자 `clamp(22~30px)`를 쓰고 있어
  `.dk-h2`(36px)와 안 맞았다. → `var(--dk-display-lg)`로 통일, 구분은 색으로만.
  모바일 26px 강제 규칙에도 kicker를 함께 물림. 실측 1440px 36/45, 390px 26/32.5 양쪽 일치.
- **CTA `문의하기` 라벨 5개 페이지 전부 제거** — 버튼에 이미 상담/문의 단어가 있어 중복.

### 위키 반영 완료 (회장 승인)
`옵시디언 위키/wiki/persona/work/`에 2건 신규 — 후보 3건 중 앞 2건이 동일 원칙이라 통합.
- `규칙은-정본에-박아야-지켜진다.md` — 좋은 규칙도 로그·기억에 있으면 안 지켜진다.
  + 딸린 실행규율 "같은 유형 결함을 두 번째 발견하면 즉시 전수 점검"
- `브랜드-인포그래픽은-소구위계를-따른다.md` — 크기는 데이터 값이 아니라 소구 강도를 따른다
`index.md`·`log.md` 갱신 완료.

---

배포 이력(`57dfbda` 시점 기준 정리). 아래 4개가 이번 세션의 핵심이고, **전부 원인이 "규칙은 있는데 안 지켜지고 있었다"였다.**

**① 디자인 시스템 특성도 버그 (가장 중요)**
tokens의 `.dk p` / `.dk h1~h6`가 특성도 (0-1-1)이라 컴포넌트 클래스 (0-1-0)를 전부 이겨서
**41개 요소가 자기 값을 못 받고 16px/1.6 기본값으로 떨어져 있었다.**
(`.dk-perf-title` 32px→16px, `.site-footer__*` 4종, `.dk-label`, `.dk-kp-text`, `.dk-pt-h` 등)
→ 기본값 선언을 `:where(.dk) p` 형태로 감싸 특성도 0으로 낮춰 해결.
**앞으로 p·h 요소용 클래스를 만들 땐 이 함정을 먼저 의심할 것.**

**② 소구 위계 — 크기를 데이터값에 묶지 말 것**
막대 길이=인원수로 매핑하면 1,500이 무조건 제일 크고 SKY 250이 제일 작아진다.
허브 다이어그램(값 비교용 도형 아님)으로 바꿔 크기를 위계에 쓰게 함.
9시간도 24시간을 분모로 두면 무조건 1/3로 작아 보여서 → 252시간(9×28일) 절대량으로 전환.

**③ 수치 귀속** — `info.md` 확인: 5,000명·재참가 60%는 **필핀+국내 합산 20년 실적**이다.
국내캠프 페이지에서 라벨 없이 쓰면 오독된다. `닥공캠프 20년 누적` / `한국캠프 …`로 구분 표기함.
근사치 표시(`약`, `이상`)를 절대 떨어뜨리지 말 것.

**④ 줄바꿈·타이포 규칙** — 좋은 규칙이 프로젝트 로그에만 있고 작업기준엔 없었다.
`tools/web/web agent/CLAUDE.md`에 **「한글 타이포·줄바꿈 기준」**으로 정본화함.
히어로 분량을 index 기준(타이틀 15자/서브 25자)으로 통일, 서브 `<br>`은 820px 이하 제거
(`<br>` 앞 공백 필수 — 없으면 모바일에서 어절이 붙는다).

**⑤ 시각 콘텐츠 슬롯** — 4개 페이지가 이미지 0%였다. 슬롯 레이아웃 5종(밴드/좌우분할/
비대칭3분할/오프셋2장/균등4열)을 만들어 배치, 데스크톱 기준 40~54% 달성.
실물이 오면 `.is-filled` 클래스 붙이고 `<img>`만 끼우면 된다. 각 슬롯에 필요한 컷과 권장 비율 명시됨.

### 이 세션에서 신설된 지침 (반드시 읽을 것)
`tools/web/web agent/CLAUDE.md`
  - **「★ 디자인 코어 준수 — 0순위 체크리스트」** 5개 항목, 전부 기계 검증 가능
  - **「★ 한글 타이포·줄바꿈 기준」** 흩어져 있던 규칙 통합
`tools/web/web agent/AGENT.md` — 절대 순서에 **7. 디자인 프리뷰 승인** 단계 신설

### 회장 개인 사항
- **회장은 색약이다.** 색으로 구분시킬 땐 명도비를 계산으로 확보(인접 최소 1.9:1)하고
  반드시 텍스트 라벨을 병기할 것. 눈대중 판단 금지.

### 남은 것
- POINT 5 전용 페이지(대치동 컨설팅) — 자료는 `닥공\부산센터 브러셔.pdf`(메이저맵).
  착수 전 회장 확인 3건: 메이저맵과 닥공의 관계 / 컨설턴트 실명·사진 게재 가능 여부 / 가격표 게재 여부
- 일과표 교시 수 vs "하루 9시간" 대외표기 충돌 — 닥공 확인 대기.
  **life-care.html 손댈 때 회장께 먼저 리마인드할 것**
- 모바일 시각 슬롯 비중 24~30%(목표 33%) — 한글 텍스트·표가 세로로 길어진 탓. 더 올릴지 판단 대기
- 개인정보처리방침 시행일·개인정보보호책임자 성명·직책

---

## 2026-07-28 세션 (1)

닥공 1차 피드백 반영 + 슬라이드 메뉴 정합 + 일과표 실물 반영. 커밋 `fc3df51`, 배포 완료.

**한 것**
- 홈: 히어로 문구 교체 / 브랜드 섹션 도입부 2줄 신설 + 본문 교체 / "캠프 한눈에 보기" 표 신설 /
  연혁 2026 교체(부산경남 입시컨설팅 센터·해외대학유학 진학 컨설팅) / **4대→5대 핵심역량**
- 롤링밴드 5번째 문구를 5개 페이지 전부에 추가(contact엔 밴드 없음 — 정상)
- 슬라이드 메뉴: 패널 안 로고·닫기(×) 신설, 닫기 3경로(×/바깥클릭/Esc) 신설, 항목 가운데정렬
- 생활환경 일과표 [TODO] → 실물 3종(월간 예시·중등부·고등부)
- CSS 버그 2건 수정(아래 "함정" 참조)

**다음 세션이 이어받을 것**
1. **POINT 05 전용 페이지 제작** — 어바웃캠프 5번째 페이지. 자료 = `C:\Users\ADMINY\Desktop\닥공\부산센터 브러셔.pdf`
   (메이저맵 컨설팅 / MAJOR MAP). 만들 때 ①홈 POINT 05 `href`(현재 `#`) ②6개 파일 헤더·패널 드롭다운
   ③`README.md`를 같이 고칠 것.
   - **착수 전 회장 확인 필수**: 메이저맵은 닥공과 로고·주소·메일이 전부 다른 별개 브랜드다(제휴/입점/계열 미확인).
     브로셔엔 컨설턴트 6인 실명·얼굴사진과 가격표 10종이 있어 게재 가능 여부를 먼저 물어야 한다.
2. **레이아웃 표현 강화(2단계, 회장 답변 대기 중)** — 표/영상자리/POINT시각/인포그래픽 4종 컴포넌트 개선안까지
   보고했고, 그중 "사선 섹션 경계 + 대형 아웃라인 워드마크"는 필핀에 없는 구조라 원칙을 푸는 결정이 필요해 보류 중.
3. 아래 "미확인" 2건(개인정보처리방침 시행일·책임자)은 여전히 미해소.

---

## 지금 상태 (요약)

- 6페이지 정적 사이트, 실제 배포됨: https://dakgong-domestic.netlify.app
- 최신 배포 커밋 `7ce5177` — 정상 반영 확인됨(넷틀리파이 API + curl 직접 확인).
- **1차 시안으로 고객(닥공) 보고 예정 단계.** 이번 세션은 그 직전 마무리 작업이었음.

## 이번 세션에서 한 일 (커밋 순서대로)

1. 상단메뉴 밑줄 제거, 모바일 히어로 서브카피 줄바꿈/폰트 수정
2. 모바일 메뉴 전면 재작성 — 필핀 실측(전체화면 흰 오버레이 → 70%폭 다크 슬라이드 패널)
3. 상단메뉴 밑줄 시스템 완전 삭제(회장이 시킨 적 없는 임의 추가분 제거 — 교훈: 지시 없는 UI 디테일 임의 추가하지 말 것)
4. 연혁 필핀 실제 컴포넌트 이식, em-dash 전량 제거, 히어로 서브카피 축약, POINT섹션 정렬, FAQ 헤더 중앙정렬
5. 홈 브랜드소개 섹션 — 필핀식 3단 감성 스토리텔링 제거 → 닥공이 준 직설적 원문으로 교체
6. FAQ 카테고리라벨/제목/목록 폭 불일치 해결, 이중박스 대비 개선, 영상인터뷰 캐러셀→고정3칸 복원
7. **헤더 메뉴 구조 전면 재작성**(`def2fbf`) — `.site-nav-bar`(PC 가로메뉴, 항상 보임) + `.site-nav`(삼선이 여는 다크 슬라이드 패널, 전 폭 공통)로 완전 분리. 이전엔 PC에서 삼선 눌러도 반응 없었음(버그). 이 커밋에 contact.html 개인정보처리방침 전면 교체도 같이 들어감.
8. **`cf634a9`** — 홈 1~3섹션(숫자/연혁/영상인터뷰) 모바일 디테일:
   - 연혁(`.dk-history`) 모바일 CSS 4개 누락분 발견·수정: 래퍼 좌우패딩, 연도숫자 26→19px, 본문 15.5→14px, 점 13→11px. (필핀 원본엔 있는데 우리 site.css에 이 4줄이 통째로 빠져있던 실제 버그였음 — "차이 없다"고 잘못 판단했던 사례, 코드 diff로 검증해서 찾음)
   - 숫자(TRACK RECORD) 섹션 → 다크배경 + 원형 아이콘뱃지 + 숫자 + 설명문 레이아웃(`.dk-stat-dark-*`)으로 전환. 기존 밝은 `.dk-g4`/`.dk-stat4`는 그대로 남겨둠(life-care.html 시설 통계가 아직 씀 — 절대 같이 건드리지 말 것, 용도 다름)
   - 영상인터뷰(`.dk-vi-card`) → 재생버튼 중앙 큰 배지에서 **우상단 작은 원형 배지**로, 캡션은 카드 중앙에서 **좌하단 반투명 오버레이 바**로 재배치. PC 3칸 그리드 / 880px 이하 모바일 1칸 수직. (필핀 실사이트는 아임웹 기본 `gallery2` 위젯이라 정확한 재생버튼 px값은 못 뽑음 — 위치·구성만 재현)

## 2026-07-30 세션 — PC/모바일 레이아웃 정리 + 인포그래픽 4차 반복

**배포 완료** `d3bd163` (커밋 3개 묶음: `1f5a567` 핸드오프 · `52c0416` PC 레이아웃 · `d3bd163` 여백 스케일)
라이브 검증: tokens/site.css에 `--dk-gap-*` 반영 확인, 6페이지 전부 200, contact `dk-ph` 잔여 0.

### 코드로 반영된 것 (배포됨)

1. **섹션 내부 여백 스케일화** — 회장 지적 "섹션내에서 상하여백 규칙 개판".
   실측 결과 섹션 내부 간격이 **0~46px 사이 16종**. 원인 두 개:
   ① 역할이 같은데 clamp 범위만 다른 CSS 선언 10종 ② HTML 인라인 margin 20개.
   → tokens에 **5단 스케일**(`--dk-gap-xs/sm/md/lg/xl`) 신설, CSS 26곳 + 인라인 22곳 치환.
   **결과 16종 → 5종.** 모바일 12/16/26/40/56 · PC 12/20/34/56/80.
2. **PC 레이아웃** — 슬롯 블록이 `<section>` 없이 `.dk-wrap`만 걸쳐 섹션 리듬을 못 받고 있었다
   (간격 0/20/84px 제각각). `.dk-ph-block` 신설해 15곳 적용. 최대 블록 높이 836→704px.
3. **모바일 슬롯 배치 버그 3건** — `.dk-ph-tri.rev` 계단식 어긋남 / `.dk-ph-split.rev` 이미지 890x72 눌림 /
   `.dk-vi-grid` 1열에서 2px 붕괴. 상세는 아래 "함정" 절.
4. **CTA 버튼 폭 통일** — '상담 신청'(4자)과 '카카오톡 상담'(6자)이 auto 폭이라 달랐다.
   `inline-grid` + `grid-auto-columns:1fr`. 전 페이지 144x44 동일, 480px 이하는 1열 320px.
5. **contact 시각 슬롯 제거** (회장 지시). `dk-ph` 잔여 0.
6. **1280px 가로 스크롤 6px** (기존 결함) — `[open]` 상태 `+` 아이콘의 45도 회전이 레이아웃 폭에
   반영되지 않아 바운딩 박스만 10px 커짐. 1280px 이상에선 `.dk-wrap` 패딩이 0이라 그대로 스크롤이 됨.
   아이콘을 24px 정사각으로 고정해 해결.

### 코드 반영·배포 완료 (2차 배포분)

**최종 배포 `3e52724`** — 커밋 11개. 7페이지 전부 200 확인.
`f2ed734` 컨설팅 페이지 신설 + 벌집 인포그래픽 + PC 가운데정렬 버그
`fd20ca0` 회장 지적 4건 · `89c53c7` POINT 좌우교차 · `a91ca29` 별도 서비스 성격 ·
`75030d1` 원문 밖 정보 6건 제거 · `74bed9c` 모바일 벌집 복구

1. **커리큘럼 「6대 핵심 수업 구성」** — 아코디언(`dk-course-list`) 폐기 →
   **벌집(육각형) 인포그래픽 + 상세 6칸 통일 템플릿**. ★현재 배포된 최종 형태다.
   · 중앙 육각형에 섹션 제목, 둘레 6개에 6대 핵심(시계방향 원문 순서). 각 육각형의 컬러 쐐기를
     **중앙 보는 모서리로 몰아** 6개가 모여 중앙을 감싸는 육각형 링이 된다. 아이콘은 쐐기 안,
     텍스트는 흰 부분. 구현 = 중심 기준 회전 반평면(`--ang` 하나로 6방향).
   · 쐐기 색 = 운영 방식(코발트 수학 / `#2a2a2a` / `#5c5c5c` / `#8f8f8f`). 링 이웃 명도비
     2.07~2.15:1(색약 기준 1.9 초과), 흰 아이콘 대비 최저 3.23:1, 범례에 글자 병기.
   · **모바일(≤880px)**: 육각형 구조를 유지하고 육각형 폭을 `(100vw-24px)/3`로 잡는다.
     칸 안에는 **번호 + 제목**만 남기고(라벨·설명·아이콘 감춤) 제목의 **뒷부분은 `.dk-hx__tl`로 감춘다** —
     지어낸 축약이 아니라 원문 단어를 자른 것: `국어 · 영어(특강)` / `수능 대비(집중반)` /
     `과학 · 사회(선택과목)` / `SKY · 의대(선배 멘토링)` / `(관리형) 자기주도학습`.
     실측 320~880px에서 **육각형 사선 밖으로 나간 칸 0건**, 제목 전부 1줄(11~14px).
   · 벌집 배경은 **`.dk-wrap` 밖(섹션 직계)** 에 둬서 풀블리드.
   · 상세 6칸 = 하나의 템플릿(상단 3px 층색 실선 + 번호·아이콘·제목·라벨 + 구분/내용 3행).
   · ⚠️ **한 번 아크+알약형 구조로 갈아엎었다가 회장 지시로 되돌렸다**(`afd323c` → `3e52724`).
     회장: *"구리다…걍 육각형 버전으로 되돌려라"*. **벌집이 확정 형태다. 다시 바꾸지 말 것.**
     (아크 구조를 다시 제안하지 않는다. 모바일 제약은 위 방식으로 해결돼 있다.)
2. **입시컨설팅 페이지 신설 `consulting.html`** — 아래 "입시컨설팅" 절 참조.
3. **메뉴** — ABOUT CAMP 하위 6번째 `입시 컨설팅` (6페이지 × PC바/슬라이드 = 12곳).
   기획안 v2의 "메뉴 5칸" 판단은 자료가 없어 페이지를 안 열기로 했던 것이므로 회장 승인으로 뒤집었다.
4. **index POINT5** `자세히 보기` 빈 링크(`#`) → `consulting.html`.
5. **POINT 좌우 교차 복구** — `fc3df51`(4대→5대 확장)에서 POINT05를 POINT04에서 복사하며
   `dk-pt-item--rev`를 지우지 않아 04·05가 연달아 좌측이었다. 01 우 / 02 좌 / 03 우 / 04 좌 / 05 우로 복구.

### ★입시컨설팅 페이지 — 확정 사항 (닥공 통화 + 회장 지시)

- **성격**: 캠프 비용에 **포함 아님. 별도 유료 서비스.** 단 **유료라는 사실을 페이지에 노출하지 않는다.**
  POINT5에 두는 이유 = 캠프 전문성 제시 + **간보기**. "닥공의 전문 비즈니스 영역 중 하나"로 쓴다.
  별도 절차임은 `학생 개별` · `신청` 같은 단어로만 읽히게 한다.
- **명시 문구**(회장이 직접 준 문장, 01 WHY 아래 중간 여백에 12.5px 회색으로):
  `(해당 서비스는 캠프 이후 별도 신청자 한정으로 진행되는 서비스로 개별 문의 부탁드립니다.)`
- **CTA 층위 분리** — 컨설팅 문의(`중등 입시 컨설팅 문의` / `고등 입시 컨설팅 문의`)는
  **03 PROCESS 직후 본문 안 흰 카드**, 캠프 문의는 **페이지 끝 풀블리드 코발트**(6페이지 공통).
  하단 공통 CTA를 컨설팅으로 교체하는 안은 채택하지 않았다 — ①6페이지 공통 CTA가 깨짐
  ②캠프 문의 경로 소실 ③유료 서비스를 결론 자리에 놓으면 판매 페이지가 되어 "간보기" 성격과 어긋남.
- **자료 원본**: `C:\Users\ADMINY\Desktop\닥공\부산센터 브러셔.pdf` (6장, 전부 이미지형 → PyMuPDF 렌더 후 판독)
  · 쓴 것 = p2 학생관리시스템 3축 · p4 프로세스 4단계+2 · p5 준비 서류
  · 뺀 것 = 메이저맵 브랜드·로고·주소·이메일·QR · 컨설턴트 6인 실명·사진 ·
    상품 10종 가격표(30만~770만) · 접수 퍼널 · p3 진학실적(메이저맵 실적이라 닥공 실적으로 오독)
  · **계약 완료 시 메이저맵 브랜드를 05 SYSTEM 자리에 한 덩어리 추가**하면 됨. 구조 변경 불필요.
- 구성: 히어로 → 01 WHY → 02 SYSTEM(3축 수렴 인포그래픽 + 상세 3칸) → 03 PROCESS(4단계+2) +
  컨설팅 문의 → 04 PREPARE → 공통 캠프 CTA

## 알아야 할 함정 (Gotcha)

- **git push는 매번 회장 명시 승인 필요.** ("배포해줘"/"반영해" 등 명확한 말이 있을 때만 push. 로컬 커밋은 자유.)
- **승인이 있어도, 곧 추가 수정이 생길 것 같으면 push 직전에 한 번 더 물을 것.**
  특히 ①내가 올린 확인 요청 답변이 아직 안 왔거나 ②후속 작업이 바로 이어질 때.
  "지금 나갈까요, 확인 받고 묶어서 나갈까요?" — 크레딧은 배포 **횟수** 단위라 모아서 한 번이 항상 유리하다.
  (2026-07-28 회장 지적: 확인 6건 띄워둔 채 배포해서 2회 배포가 될 뻔함.)
- **넷틀리파이가 최근 크레딧제로 바뀜**(빌드분 아님). 무료 300크레딧/월, 배포 1회=15크레딧. 2026-07-26에 무료 크레딧 소진으로 배포가 안 걸리는 사고 있었음 → 회장이 Personal $9/월(1,000크레딧) 업그레이드로 해결. **크레딧은 "배포 횟수" 단위로만 소모되고 변경 파일 개수와 무관** — 수정 여러 개 모아서 한 번에 push하는 게 유리.
- **배포 확인은 브라우저 도구 대신 이 방법이 확실함**(이 환경에서 브라우저 도구가 netlify.app/localhost 접근이 막혀있음):
  - `curl -s https://dakgong-domestic.netlify.app/assets/css/site.css`로 실제 배포된 코드 직접 확인
  - Netlify MCP: `netlify-project-services-reader`(`get-project`, siteId=`28e27695-2535-4e21-889a-54c68eae7d3b`)로 `currentDeploy.id` 확인 → `netlify-deploy-services-reader`(`get-deploy-for-site`)로 `commit_ref`가 최신 커밋인지 대조
  - git push 후 자동배포가 안 걸리면(크레딧 등 이슈) `git commit --allow-empty` + push로 재트리거 가능. **`netlify-deploy-services-updater`의 `deploy-site`(CLI zip 업로드 방식)는 이 git-연동 사이트에서 계속 400 Bad Request로 실패함 — git push 방식만 쓸 것, CLI zip-deploy는 이 사이트에서 안 됨.**
- **"차이 없다"고 말하기 전에 반드시 코드 diff로 확인.** 스크린샷만 보고 판단하면 실제 CSS 누락(연혁 모바일 사례)을 놓친다. 필핀 원본은 `C:\Users\ADMINY\Desktop\claude code\clients\dakgong\imweb\_reference\home-live-source.html`(curl로 떠둔 실사이트 원본).
- 필핀 실사이트 IP를 색상만 바꿔 재사용하는 건 같은 법인(HMJ) 소유물이라 승인된 방식임(제3자 저작물 아님). 단 필핀 "리얼 피드백" 카톡 캡처 후기 7건은 **게재 금지 확정**(2026-07-17 회장 판정) — 절대 재사용하지 말 것.
- 사실·통계·후기 지어내기 절대 금지. 불확실하면 `[준영확인]` 표시.
- **CSS: p 요소용 클래스는 반드시 `.dk` 를 앞에 붙일 것.** `tokens-domestic.css`의 `.dk p`가 특성도 0-1-1이라
  클래스 하나짜리(0-1-0) 규칙을 이겨서 그냥 쓰면 조용히 안 먹는다(2026-07-28에 `.dk-table-note`가
  만들어진 이래 한 번도 적용된 적 없었다는 걸 발견). 기존 관례 = `.dk .lead`.
- **CSS: 미디어쿼리는 특성도를 안 올린다.** 파일 앞쪽 `@media(min-width:1280px){...}`에 넣은 규칙이라도
  그 뒤에 같은 선택자를 다시 선언하면 뒤엣것이 이긴다(`.dk-pt-in` 폭 어긋남의 실제 원인이었음).

- **여백도 토큰이다.** 색을 토큰에서만 고르듯 여백도 `--dk-gap-xs/sm/md/lg/xl` 5단에서만 고른다.
  `clamp()`를 매번 새로 쓰는 게 가장 흔한 오염 경로다(역할이 같은데 범위만 다른 선언이 쌓인다).
  **HTML 인라인 `style="margin-..."`은 금지** — 여기가 잡값의 주 통로였다.
- **여백을 측정할 땐 모션을 끈다.** `data-reveal` 트랜지션이 0.7s라 그 중에 재면 ±4px 잔상이 섞여
  잡값 5종이 12종으로 보인다. Playwright `reduced_motion='reduce'`로 재야 정확하다.
- **반응형에서 PC 배치를 되돌릴 땐 "부분 초기화" 금지.** 같은 요소에 건 `grid-column`/`grid-row`/`height`를
  **전부 한 번에** 초기화하고 새로 배치한다. 그리고 **초기화 선택자의 특성도를 원본과 맞춰야 한다** —
  PC 규칙이 `.a.rev > *:nth-child(2)`(0,3,0)인데 리셋을 `.a.rev > *`(0,2,0)로 쓰면 조용히 무시된다.
  `> *:nth-child(n)`으로 맞출 것. (모바일 tri가 계단식으로 어긋난 실제 원인)
- **`order`는 순서만 바꾸고 컬럼 폭은 안 바꾼다.** 좌우 반전(`.rev`)을 만들 땐 `grid-template-columns`도
  함께 뒤집어야 한다. 안 하면 이미지가 캡션 폭(340px)에 들어가 890x72로 납작해진다.
- **`transform`은 레이아웃 폭에 반영되지 않는다.** 45도 회전 아이콘은 바운딩 박스만 커져 컨테이너 밖으로
  나간다. 회전시킬 요소는 **고정 정사각 박스**로 묶고 여유 패딩을 준다.
- **비율 상한을 `max-height`로 걸지 말 것.** `aspect-ratio`가 걸린 칸은 높이를 누르면 폭도 같이 줄어
  컬럼보다 좁아지고 옆에 죽은 공간이 생긴다(1440px에서 780px 컬럼에 카드가 698px). → **더 완만한 비율**로 바꾼다.
- **`clip-path`를 가진 요소에 `filter`를 같이 걸지 말 것.** 자식(쐐기 등)이 클립을 못 받고 밖으로 나간다.
  **그림자는 부모에** 걸고 clip은 자식에 둔다.
- **★히어로는 절대 새로 만들지 않는다.** 실사이트는 홈과 하위 5페이지가 **완전히 같은 컴포넌트**
  (`.dk-bg-video-section` + `.dk-bg-video-placeholder` + `.dk-bg-video-content` 안의 `h1` + `p.lead`)를 재사용한다.
  이건 2026-07-25에 회장 지적으로 통일해 둔 것이다. **브레드크럼·라벨·킥커는 실사이트 히어로에 0건** —
  넣으면 안 된다. 값: min-height 600(768px 이하 520) · 패딩 80px 0(768px 이하 72px 24px) · max-width 1280 ·
  h1 Paperlogy 30px/700 ls-0.3(768px 이하 22px) · lead Pretendard 18px/400(768px 이하 16px/1.6).
  (2026-07-30 회장 지적: "히어로 섹션 디자인 또 지맘대로 바꿔놨네, 다른페이지들 하고 통일안하고")
- **★카드를 버리지 말 것 — "카드만" 쓰는 게 문제였다.** `site.css` 실측: radius-lg(16px) 13회 · md 8회 ·
  full 9회 · box-shadow 16회. **라운드 카드가 이 사이트의 기본 언어다.** 순수 편집형(라운드 0·상자 0)으로
  밀면 나머지 5페이지와 결이 깨진다(2026-07-30 회장 지적). 남는 금지는 하나뿐 —
  **라운드 + 컬러 좌측 레일 동시 사용**(`anti-ai-slop` P0 #5). 강조는 **상단 실선**으로.
  ⚠️ 실사이트 `.dk-callout`이 지금 이 금지를 위반 중(라운드 + `border-left:4px`) — 정리 대상.
- **★병렬 항목은 레이아웃을 통일한다.** 같은 종류의 항목 N개(6대 핵심 등)를 항목마다 다른 구조로 짜면
  병렬성이 깨져 세트로 안 읽힌다. **다양성은 페이지의 서로 다른 섹션 사이**에 두는 것이고 병렬 세트 안이 아니다.
  (2026-07-30 회장 지적: "6대 수업의 상세내용들 섹션 디자인 레이아웃은 통일시켜야 맥락이 맞다")
- **레퍼런스를 받으면 "요점"을 먼저 말로 정리할 것.** 벌집 레퍼런스를 3번 틀렸다 —
  육각형을 늘어놓기만 하고 ①중앙에 제목 ②쐐기를 중앙 보는 모서리로 몰아 링을 만드는 것
  ③아이콘은 쐐기 안, 텍스트는 흰 부분 이라는 요점을 놓쳤다.
- **★자료를 못 찾겠으면 저장소 밖도 볼 것.** 컨설팅 자료가 `Desktop\닥공\` 폴더에 있었는데
  `clients/dakgong/`만 뒤지고 "자료가 없다"를 반복했다. 회장이 준 첨부는 대화 기록
  (`~/.claude/projects/.../<session>.jsonl`)에서 파일 경로째로 검색할 수 있다.
- **★회장이 준 카톡·메모는 "지시"가 들어 있다고 보고 두 번 읽을 것.** 컨설팅 카톡에서
  이사님이 "메이저맵은 빼고 작업" = 회장이 제시한 두 갈래 중 **"닥공이 직접 제공하는 것처럼
  내용만 발췌해서 활용"을 승인**한 것이었는데, 나는 "제외"만 읽고 **"내용은 쓴다"를 놓쳤다.**

- **★★인포그래픽 구조를 고르기 전에 가장 좁은 폭(320px)에서 칸 폭을 먼저 계산한다.**
  벌집은 3열을 강제해 390px에서 칸이 114px다. 원문 제목을 그대로 넣으면 사선 밖으로 나간다.
  ⚠️ 그래서 아크+알약형으로 갈아엎어 봤지만 **회장이 "구리다"며 벌집으로 되돌리라 했다.**
  → **형태는 회장이 정한다. 실행층은 제약을 계산해서 "이 형태에서 이만큼만 들어간다"를 보고하고,
  담는 내용을 줄이는 방식으로 맞춘다.** 구조를 임의로 갈아엎지 않는다.
  벌집의 해법 = 번호+제목만 남기고 제목 뒷부분을 감추기(원문 단어를 자름).
  · 칸 폭 = (100vw − 좌우패딩) ÷ 열수. 한글 제목 1줄은 **칸 폭 ÷ 글자수 ≥ 12px**이어야 읽힌다.
  · 이 계산이 안 맞으면 그 구조는 쓰지 않는다. 텍스트를 지우는 걸로 해결하지 않는다.
- **★단색 톤 변주는 4단이 안 나온다.** 같은 hue로 인접 명도비 1.9:1을 4단 확보하려면
  L이 0.03/0.10/0.24/0.50까지 벌어져야 해서 브랜드색 범위를 벗어난다.
  **2단 교대(진/연) + 나머지는 라벨 글자로** 구분하는 게 색약에도 안전하고 브랜드도 안 깨진다.
- **★`@media` 안에 선택자 없이 선언을 쓰면 그 블록 파싱이 깨진다.** `--cut: 63%;`를
  `@media{}` 직계에 써서 뒤따르는 `display:none` 규칙이 무시됐고, 숨겼어야 할 텍스트가 나와 넘쳤다.
  CSS를 스크립트로 조립할 때 특히 쉽게 생긴다 — **전 파일 정규식 검사로 잡을 수 있다.**
- **★★모바일이 기준이다. 모바일부터 짜고 PC로 넓힌다.** 이번에 벌집 인포그래픽을 PC 전용으로 짜고
  ≤880px에서 `clip-path`를 없애 **평범한 카드 목록으로 떨어뜨렸다** — 모바일에서 인포그래픽이 통째로 사라졌다.
  회장: *"항상 모바일이 메인인데 왜 니맘대로 pc에만 적용한거야"*.
  **좁은 폭에서 안 들어가면 형태를 버리는 게 아니라 담는 내용을 줄인다.**
  벌집은 육각형 폭을 `(100vw - 24px)/3`로 잡고 둘레 칸을 [번호+아이콘]만 남겨서 360px까지 살렸다
  (텍스트는 바로 아래 상세 6칸이 담당, 번호 01~06이 1:1로 이어짐).
  ⚠️ **검사 항목에 "좁은 폭에서 형태가 유지되나"를 넣는다** — `clip-path`가 `polygon`으로 남아 있는지 등.
- **★공통 요소는 타이핑하지 말고 파일에서 복사한다.** 이번 세션에서 같은 실수를 **두 번** 했다 —
  ①히어로를 `.chero`로 새로 만들었고 ②롤링밴드 5개 문구를 새로 써서 컨설팅만 달랐다
  (`하루 9시간 이상 압도적인 수학 학습량` → `수학 중심 하루 9시간 이상 학습량`으로 변형).
  **CSS도 문구도 기존 파일에서 그대로 떠 온다.** 검증은 6페이지 해시 비교로 한다.
- **★`margin: 0`으로 `margin: 0 auto`를 죽이지 마라.** `.dk-ph-block`에 상하 여백을 주려고
  `margin: 0`을 썼는데 `.dk-wrap`의 가운데 정렬이 함께 죽어 1280px 넘는 화면에서 슬롯이 좌측에 붙었다
  (1900px 실측: `.dk-wrap` 좌310/우310 vs 슬롯 좌0/우620). 상하만 필요하면 **`margin-block`** 을 쓴다.
- **★검사 항목에 "가로 위치(좌우 여백 대칭)"를 반드시 넣는다.** 간격·가로스크롤만 재면 위 버그를 못 잡는다.
  `.dk-wrap`/`.dk-pt-in`의 `left`와 `innerWidth-right`를 비교해 4px 초과면 실패로 본다.
- **★검사 항목에 "글자색 = 배경색"을 넣는다.** `.dk a`(0-1-1)가 `.dk-cinq__b`(0-1-0)를 이겨
  코발트 글자 + 코발트 배경으로 **버튼 글자가 완전히 안 보였다.** `p`만 조심하면 되는 게 아니라
  **`a`도 같다** — 링크성 클래스는 `.dk` 접두를 붙여 0-2-0으로 올린다.
- **★풀블리드는 음수 마진으로 만들지 말고 `.dk-wrap` 밖으로 뺀다.** ≥1280px에서 `.dk-wrap` 패딩이 0이라
  파고들 여백이 없어 그대로 문서 밖으로 나간다(실측 1280px 가로 48px).
- **★다크섹션 전용 클래스를 밝은 섹션에 쓰지 마라.** `.dk-kp-note`는 `text-align:center` + 흰 글자다.
  밝은 섹션에 쓰면서 인라인으로 색만 덮어쓴 게 캡션이 가운데 정렬로 뜬 원인이었다.
  **인라인 색 override가 보이면 클래스 오용 신호다.** → `.dk-ph-note` / `.dk-table-note`로.
- **★"지어낸 정보 없나"를 스스로 먼저 검사한다.** 이번에 회장이 물어서야 **6건**을 잡았다 —
  롤링밴드 변형 · `전 학년`(원문에 대상 표기 없음) · `반드시`(원문 "필요한") · `최근 응시한`(조건 없음) ·
  `학년에 따라 설계가 달라지므로`(근거 없음) · `10년 이상`(메이저맵 연수).
  **검사법**: 페이지 본문 문구를 전부 뽑아 원문 조각과 1:1 대조하고, **조건·강도·연수·수량**을
  붙이지 않았는지 본다(`반드시`·`최근`·`전`·`10년` 같은 말이 원문에 있었는지).
- **★블록을 복제할 때 상태 클래스를 지운다.** POINT05를 POINT04에서 복사하며 `dk-pt-item--rev`를
  안 지워 좌우 교차가 깨진 채 여러 세션을 갔다. `--rev`·`--active`·`open` 같은 상태 클래스는 복제 후 점검.

## 미확인 / 회장 답변 대기 중

- `contact.html` 개인정보처리방침 제13조 **시행일** — 현재 `[준영확인: 시행일]` placeholder
- 개인정보처리방침 **개인정보보호책임자 성명·직책** — 현재 `[준영확인]` placeholder
- 이번에 배포한 시안(커리큘럼 벌집 + 컨설팅 페이지)에 대한 닥공 측 피드백
- **컨설팅 브러셔 p3 진학 실적(연세·서울대·의예과 등)** — 실행층 판단으로 제외 상태 유지.
  메이저맵 실적이라 브랜드를 지운 채 실으면 닥공 실적으로 읽힌다. (p2의 "10년 이상"도 같은 이유로
  회장 판단에 따라 연수만 제거함 — 2026-07-30)
- **메이저맵 정식 계약 여부** — 계약되면 브랜드를 컨설팅 페이지 05 SYSTEM 자리에 추가.
- 컨설팅 CTA 링크가 현재 둘 다 `contact.html`이다. **중등/고등을 분리 접수**하려면
  별도 폼 또는 문의 유형 파라미터가 필요하다(닥공 확인 필요).

## 다음 세션 첫 행동

0. **`tools/web/web agent/CLAUDE.md`의 「★디자인 코어 준수 0순위 체크리스트」와
   「★한글 타이포·줄바꿈 기준」을 먼저 읽는다.** 2026-07-28에 신설됐고, 이걸 안 읽으면
   같은 실수(색 지어내기·특성도 함정·줄바꿈 방치)를 그대로 반복하게 된다.
1. 회장이 닥공 피드백을 전달했는지 먼저 확인 — 있으면 그것부터 처리.
2. **커리큘럼 벌집 + 컨설팅 페이지는 이미 반영·배포 완료**(`75030d1`). 다음은 닥공 피드백 처리다.
   컨설팅의 성격 규정은 위 "★입시컨설팅 페이지" 절을 먼저 읽을 것 —
   **캠프 비용 미포함 별도 유료 서비스지만 유료라고 쓰지 않는다.** 접수 퍼널·가격표 금지.
   회장이 준 명시 문구는 그대로 유지한다(문장 임의 수정 금지).
3. **`life-care.html`을 손대면** 일과표 교시 수 vs "하루 9시간" 대외표기 충돌 건을 **먼저 리마인드**할 것(닥공 확인 대기).
4. 없으면 "미확인" 두 항목(개인정보처리방침 시행일·개인정보보호책임자) 여쭤서 contact.html 마무리.
5. 새로운 디자인 디테일 지적이 들어오면, 먼저 필핀 `home-live-source.html` 실제 코드에 해당 컴포넌트가 있는지
   grep으로 확인하고(있으면 그대로 이식, 없으면 아임웹 기본위젯일 수 있음을 먼저 확인) 나서 손댈 것 — 추측성 판단 금지.
6. **눈으로 봐야 판단되는 변경은 구현 전에 프리뷰부터.** (`AGENT.md` 절대순서 7단계 — Artifact로 발행해 링크로 승인)
